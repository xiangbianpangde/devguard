# 收束报告 v2.2：ECC 全项目对标与一键初始化 2.0

> 更新: 2026-07-11
> 范围: 功能点 #50-#52 | 状态: 机器收束通过，人审待签核

## 一、整理

本轮拉取并合入本地 `master`、远程依赖版本、工作流闸门和一致性修复分支的全部历史。冲突保留当前 fail-closed 与跨平台实现；拒绝回流第二归档真源、Shell-only 仪表盘及对 LICENSE/CODEOWNERS 注入 Markdown 的旧语义。

ECC 对标覆盖 skills、rules、agents/commands 边界、hooks、scripts/install、manifest/profile、Codex/MCP、安全、CI/tests、rollback/provenance 十个能力面。项目不镜像 ECC 的大规模资产目录，只采用适合开发规范模板的机制。

## 二、测试

| 检查 | 结果 | 证据边界 |
|------|------|----------|
| TDD RED | 5 项预期失败 | AGENTS/skill、回滚、dry-run、ECC checker 尚未实现 |
| 聚焦回归 | 16/16 PASS | scaffold + ECC alignment |
| 全量 pytest | 163/163 PASS | 全仓 conventions/tests |
| 全仓 ruff | PASS | scripts + tests，顺带清理 8 个历史未使用 import |
| Markdown / render | PASS | 250 Markdown；元数据与生成 pre-commit 同值 |

## 三、审计

| 门禁 | 实测 | 阈值 | 结论 |
|------|------|------|------|
| 一致性事实矩阵 | 24/24 = 100% | >=80% | PASS |
| 强制性故障注入 | 10/10 = 100% | >=90% | PASS |
| ECC 十域对标 | 10/10 = 100% | >=80% | PASS |
| 全量测试 | 163/163 | 0 fail | PASS |

安全检查包含本仓 gitleaks 与生成项目 core gitleaks。项目 `.codex/config.toml` 不含 MCP 服务、token、API key 或 password，多代理默认关闭，避免一键初始化修改用户全局能力与凭据边界。

## 四、效果验证

在仓库外随机临时目录执行真实 core `--dry-run` 与 `--install`：dry-run 未创建目标；安装生成 22 个 manifest 文件，创建 `.venv`、初始化 Git、安装 pre-commit/commit-msg，并把既有 ECC/global hooks 与项目 hooks 组合。目标自检 `DEVGUARD OK`，目标测试 2/2，通过 9 项完整 pre-commit（large-file、JSON、YAML、EOF、空白、ruff、ruff-format、gitleaks、DevGuard verify）。8 个关键跨 Harness/Hook 文件零缺失，项目 `core.hooksPath` 指向实际 `.git/hooks`。

回滚故障注入在第三次原子写入失败：Owner README 恢复原字节、无关文件保持、已创建治理文件与唯一临时文件全部清除。

## 五、技术债

| 项目 | 状态 | 影响 |
|------|------|------|
| v0.2-v2.2 人审计签核 | 16 个节点待 Owner | 不影响机器门禁；不得表述为人审通过 |
| `--install` 的 pip/venv 外部副作用 | 安装阶段失败时保留诊断现场 | manifest 写入事务安全；未来可增加 `--cleanup-on-install-failure` |
| 远程单分支收敛 | 在最终提交后执行 | 以 GitHub 远程引用回读为最终证据 |

## 收束结论

\#50-#52 的本地实现、故障注入、仓外 E2E 和量化门禁全部通过。V2.2 标记为机器收束通过；人审签核与远程单分支状态保持独立证据边界。
