# 红队对抗审查 · 第二轮问题清单

> 更新: 2026-08-14
> 性质：红队第二轮问题清单（3 天赛制第 1 天，波次推进）。红队全程只读，未修改任何仓库文件。
> 审查快照：**HEAD = 3cb59ef**（含蓝队情报包落地：N-1 extend 改造 / N-2 SECURITY.md / S-2 llms.txt / 判定书归档）；蓝队工作区在途改动（CI 钉版、render.py 转义）单独标注，未入库不予验收。
> 攻击面来源：GitHub 同类项目情报 + 本地 easy-vibe 对照 + 红队主动侦察（供应链 / CI / 平台层 / 渲染链 / 豁免语义）。
> 红队：队长（019ff632）交叉 + 成员 019ff62f（A2-01~04）+ 成员 019ff62d（A1~A5）。

## 一、总览

| 严重级 | 数量 | 条目 |
|--------|------|------|
| P1 | 3 | R2-01 豁免到期语义放行 / R2-02 渲染零转义 / R2-04 CI 运行时供应链 |
| P2 | 4 | R2-03 远程安装无校验 / R2-05 CI 权限与 action 钉版 / R2-06 pre-commit tag 供应链 / R2-07 平台层文档声称漂移 |
| P3 | 1 | R2-08 ADR 0009 可执行性缺口 |

另：**通过项**（N-1 extend 改法红队实测有效）；**过程记录**（红队拦截蓝队在途无效 SHA 一次）。

---

## 二、P1（3 项）

### R2-01 · C6 · 豁免到期「语义放行」：无限期豁免可合法登记

- **证据**：`scripts/check_exemption_log.py:78` `EXPIRY_RE = ^\d{4}-\d{2}-\d{2}$`；`:97-106` `validate_expiry` 仅校验非空+格式匹配，**无「到期 ≥ 今天」与「有效期上限」语义检查**
- **复现**（实跑）：构造含 `2099-12-31` 与 `1900-01-01` 到期的豁免记录 → 均 PASS（0 错误）——任何提交者可登记 2099 年到期=无限期豁免；已过期记录同样不被拒绝
- **实际/期望**：C6「有到期」应含语义（今天 ≤ 到期 ≤ 合理上限如 180 天），超限或过期即 FAIL
- **存量审计**：既有 3 条到期均合理（1/2=当天一次性、3=30 天），存量合规；漏洞在 checker 语义层
- **修复建议**：格式校验后加语义界校验 + 违规 FAIL；收束审计复核超长豁免

### R2-02 · S7 · 渲染零转义：STATUS/_meta 任意值可注入 dashboard.html

- **证据**：`docs/templates/devguard/html-report-template/render.py` `render_convention_rows`（L90-108）与 `render_status_rows`（L112-128）以 f-string 直接拼 `title/l1/l3/status/num/name/bdd/date` 进 `<td>`，**全文 0 处 html.escape**
- **复现**（临时副本实测）：STATUS.md 注入 `<script>alert(1)</script>`、`<img src=x onerror=alert(2)>` → 渲染产物原样直出，无任何转义
- **威胁链**：PR 改 STATUS.md 状态列 → 合入 → CI 渲染 dashboard.html → 打开即执行注入 JS
- **期望**：所有插值经 `html.escape`；补注入 fixture → 产物无原始 `<script>` 的回归测试
- **注**：蓝队工作区已有 render.py 转义改动（在途未提交），入库后红队复验

### R2-04 · C3 · CI 运行时供应链：浮动依赖 + npx latest 绕过 lock

- **证据**：`ci.yml:37/195` `pip install pyyaml` 浮动；`:63` `pip install import-linter` 浮动；`:68` `npx --yes @stoplight/spectral-cli` 取 latest、**完全不在 package-lock**；`npm ci`(:57) 使用 lock v3 且 171 包全有 integrity（正向✅），但 spectral 绕过锁
- **实际**：CI 每次运行依赖上游最新版=不可复现+供应链执行风险
- **期望**：spectral 入 package.json/lock + `npx --no-install`；pyyaml/import-linter 钉版或 `--require-hashes`
- **修复建议**：全部依赖进入 lock 或精确钉版

---

## 三、P2（4 项）

### R2-03 · D1 · 远程安装无完整性校验 + master raw 未钉版

- **证据**：README:71-84 官方推荐 `curl -fsSL .../master/scripts/install.sh | bash`；install.sh/install.ps1 全文无 sha256/signature 校验（receipt sha256 是写后完整性，非下载源验证）；v2.5.0 Release 无任何 assets（GitHub API 确认）
- **威胁模型**：raw.githubusercontent 投毒（仓库接管/CDN 污染/上游失陷）→ 用户执行恶意代码；且指向 **master** 而非 release tag——每次 push 都改变安装内容
- **期望**：README 提供固定版本 SHA 清单或签名校验；主路径改 `git clone` + 本地执行，curl|bash 降级为「已核验 SHA 的高级用法」
- **缓解现状（肯定）**：fail-closed 设计（无法自我定位时提示 clone，不静默降级）

### R2-05 · C3 · CI 权限未最小化 + action tag 钉版（在途修复中）

- **证据**：HEAD 3cb59ef 的 ci.yml 无 workflow 级 `permissions`；actions/checkout@v4、setup-python@v5 以 movable tag 钉版（5 job 各重复）
- **在途状态**：蓝队工作区已加 `permissions: contents: read` + SHA 钉版；**但首版 SHA 无效**（API 422，红队拦截预警），蓝队已按红队提供值修正为真实 SHA（checkout@`11d5960a3267...`、setup-python@`a26af69be951...`）——未提交，入库后红队复验
- **过程记录**：在途无效 SHA 若入库将致 CI 全失败，属防御提交质量事故，红队预警后即时改正
- **修复建议**：另评估 `persist-credentials: false`（checkout 默认持久化 token 到本地 git 配置）

### R2-06 · C2 相邻 · pre-commit 第三方钩子供应链：movable tag 钉版

- **证据**：`.pre-commit-config.yaml:12-35` 3 个远程 repo 均 tag 钉版（pre-commit-hooks v5.0.0 / ruff-pre-commit v0.15.20 / gitleaks v8.24.3），无 commit SHA；scaffold 同款
- **威胁模型**：上游 tag 被移动 → 开发者机器与初始化项目执行恶意 hook（代码执行级）
- **期望**：真源渲染改 40 位 SHA + 注释版本号；加 immutable-rev checker；或接受风险并文档化（需 Owner 决策）

### R2-07 · 平台层文档声称 vs API 事实漂移

- **证据**（公开 API，无 token）：master 分支 protected=true，required contexts 恰为 5 个：L1 lint / pytest / L4 convention / Compliance / Build；跨平台 job 的 check runs 存在但**未直接列入 required contexts**（经 Build needs 间接阻断）——SECURITY.md:25/28 声称 required checks 含跨平台，与 API 事实不完全一致
- **不可验证项**：`/branches/master/protection` 细节（401）、push protection endpoint（404）、Dependabot（401）——无 token 均无法确认；**SECURITY.md 不应声称已启用未经确认的防护**
- **期望**：Owner 携 admin token 核对后按事实修正 SECURITY.md 表述；或明确标注「待确认」

---

## 四、P3（1 项）

### R2-08 · 流程可执行性 · ADR 0009 变异要求无现成命令清单

- **证据**：08-汇报收束.md:261 §6.4 与 BDD 07 场景 6 要求「真源侧反向变异必须 rc=1 硬失败」；但 scripts/ 无变异脚本、无「变异清单」（改哪个键/什么值/期望 rc/恢复命令）
- **实际**：非本次对抗亲历者无法照做 → 条款恐成纸面（与本轮「声称与事实不符」同构）
- **修复建议**：08-汇报收束附录加「变异验证命令清单」（键名+注入值+验证命令+恢复+复跑）；或提供 `scripts/mutate_check.sh`

---

## 五、通过项与过程记录

- **N-1 extend 改法 ✅ 红队实测有效**：`.gitleaks.toml` 采用 `[extend] useDefault=true` + allowlist（append 合并）；root/template/scaffold 三份 sha256 均为 `2a989a7c...` 逐字节一致；缓存 v8.24.3 二进制 debug 确认「extending config with default config」；高熵 `github_pat_` staged 经 root pre-commit → rc=1；AWS `AKIA...EXAMPLE` allowlist → rc=0。建议补充行为级验收测试（现有 test_scaffold 仅静态检查）
- **llms.txt**：已落地（S-2），内容质量留待第二波攻击核验
- **过程记录**：红队拦截蓝队在途无效 action SHA 一次（4 组 API 422 复验），提供正确值，蓝队即改——「预警-响应」机制运转良好

## 六、复验裁定（2026-08-13，蓝队防御批 17d9d5d+8ae0ee7，master=8552309）

> 红队按波次协议逐项复验。方法：实跑边界/注入/哈希/API/变异。

### 裁定：4 闭环 / 4 部分闭环 / 新增 1（R2-09）

| R | 裁定 | 依据 |
|---|------|------|
| R2-01 | ✅ 闭环 | 六边界实跑：2099/1900/+366 天 FAIL，+365 天/当天 PASS，存量 3 条无回归 |
| R2-02 | ✅ 闭环（附建议） | 注入 fixture 重跑：产物全部 `&lt;` 转义、原始 `<script>` 残留 0；**建议补转义回归测试**（当前 tests/ 无该防回归测试） |
| R2-03 | ✅ 闭环（附提示） | README 已钉 v2.5.0 tag raw + SHA256 表；实测 install.sh `ebbfbc8b...` 与 install.ps1 `c69289f3...` 与表值一致；提示：Release 页双源需在发布流程保持一致 |
| R2-04 | ⚠️ 部分闭环 | pyyaml==6.0.3/import-linter==2.13/spectral 6.16.3 入 lock（integrity 在场）+npx --no-install ✅；**但 ci.yml:198 `pip install pyyaml` 仍未钉**，且 `_meta.yaml:491/492/502`（真源 ci 节）pyyaml/import-linter/pytest 仍浮动——**真源与产物不一致**（ci.yml 钉了、真源没钉） |
| R2-05 | ⚠️ 部分闭环 | permissions: contents:read ✅；两个 SHA 经 GitHub API 200 验证存在且根/模板六处统一 ✅；**persist-credentials:false 未加**（checkout 默认持久化 token 到本地 git 配置） |
| R2-06 | ⚠️ 条件闭环 | 三要素（理由/Owner 确认位/触发条件）齐全✅；但理由①「pre-commit 对 tag 有本地缓存，首次解析后固定」**技术性错误**——缓存只对已有缓存环境固定，新机器/新初始化项目首次解析取当前 tag 内容，tag 移动即执行恶意 hook；且 Owner 确认位仍 ⏳ 未签认 → Owner 签认+理由①修正前不闭环 |
| R2-07 | ⚠️ 部分闭环 | push protection/Dependabot 已改「未验证/待 Owner 核对」✅；但 SECURITY.md #1 承认「跨平台未直接列入 required」而 #2 仍写「Required status checks 含跨平台」——**同文档自相矛盾**，与 API 事实（恰 5 项）不符 |
| R2-08 | ✅ 闭环（附建议） | ADR-0009 附录 5 条变异命令实测第 1 条：变异 ruff→9.9.9 rc=1 → 还原 rc=0 → 无残留；其余 4 条键名/路径核对存在；建议 08-汇报收束 §6.4 加显式指引「命令清单见 ADR-0009 附录」 |
| **R2-09（新增）** | ❌ 新发现 P2 | master 上 STATUS `completed=55` vs CLAUDE.md `completed=54` → check_consistency 实测 **25/26=96.2%** > 95% 阈值 → rc=0，CI 7/7 全绿未拦截——G1 双入口漂移 + 非关键事实被聚合阈值豁免（与 R-02 假绿同族） |

### 第三轮修复要求（窄修清单）

1. **R2-04 残余**：ci.yml:198 `pyyaml==6.0.3`；`_meta.yaml` 真源 ci 节同步钉版（pyyaml/import-linter/pytest 全钉）——真源与产物必须一致
2. **R2-05 残余**：checkout 步加 `persist-credentials: false`（根+模板）
3. **R2-06 残余**：修正理由①表述（缓存仅对已有环境固定，新环境首次解析取当前 tag）；促 Owner 签认；签认前维持「条件闭环」
4. **R2-07 残余**：SECURITY.md #2 与 #1 对齐（required 恰 5 项，跨平台为间接阻断），消除自相矛盾
5. **R2-09**：CLAUDE.md 54→55 同步；并评估非关键事实失败是否应 CI 可见/阻断（防「全绿与事实 FAIL 并存」）
6. 建议（非阻塞）：转义回归测试；§6.4 显式指向 ADR 附录

## 七、第三轮复验裁定（2026-08-14，蓝队窄修 4ddb713+08ee9f9，master=c8e064a）

> 红队按第三轮窄修清单逐项复验 + 变异深验。

### 裁定：5 闭环 / 3 未闭（含 2 新发现）

| R | 裁定 | 依据 |
|---|------|------|
| R2-07 | ✅ 闭环 | SECURITY.md #1/#2 均已按平台实际措辞（恰 5 项、跨平台非 Required），自相矛盾消除 |
| R2-03 | ✅ 闭环 | SHA 表与 install 文件实测逐字节一致；第三批未触碰 install 文件 |
| R2-05 | ✅ 闭环 | persist-credentials:false 根+模板 6/6 |
| R2-09 | ✅ 闭环 | CLAUDE 55/55 已同步；变异 55→54 → rc=1 硬失败（CLAUDE projection 入 critical 组）；恢复 26/26 |
| R2-06 | ⚠️ 部分 | 三仓库 SHA 钉版真实（API 200）+ rev_sha 真源 + render_meta 渲染链 + scaffold 同步 ✅；**但 SHA 变异覆盖不全**：变异 rev_sha.pre-commit-hooks / rev_sha.gitleaks → rc=0（仅「元数据可复现 0/1」，25/26=96.2% ≥ 95 未进硬失败组）——evaluate_ci_projection 仅校验 ruff SHA（check_consistency.py:298-307），两仓库 SHA 可静默漂移 |
| R2-04 | ❌ 残余 | ci.yml 全钉 ✅；**但 `_meta.yaml:495-496`（根+模板）仍浮动 `ruff==0.15.20 pyyaml` / `import-linter`**——真源/产物不一致依旧（只钉了 :506 一行） |
| **R2-10（新）** | ❌ P1 | C6 破例未登记：worklog L26 自述「本地提交 SKIP=gitleaks（CI 兜底）」，但豁免清单无登记、commit 消息无 [skip-*]——破例三要素（记录/Owner/到期）全缺，且环境变量 SKIP 绕过了 commit-msg 豁免体系（机制脱节） |
| **R2-11（新）** | ❌ P2 | R2-06 残余：三 SHA 中仅 ruff 入硬失败组，pre-commit-hooks/gitleaks SHA 漂移在聚合阈值内假绿（与 R2-09 同族的阈值豁免问题第三次出现） |

### 第四轮窄修清单（4 项）

1. **R2-04 终修**：`_meta.yaml:495-496`（根+模板）pyyaml/import-linter 钉版（ruff==0.15.20 pyyaml==6.0.3 / import-linter==2.13）
2. **R2-06 终修**：三仓库 rev_sha 逐项校验入硬失败组（或「元数据可复现」事实整体入 critical_failures）；加三 SHA 变异测试（变异任一个 → rc=1）
3. **R2-10**：豁免清单补登记 SKIP=gitleaks 破例（日期/范围/钩子/Owner/为何/到期），或 §一 规则显式写明「环境变量 SKIP 类破例的登记边界」（二选一，需 Owner 决策痕迹）
4. **系统性（建议入技术债）**：聚合阈值豁免问题三连（R-02 工具链 / R2-09 CLAUDE / R2-11 SHA）——建议将「结构性同步/投影事实」全组纳入硬失败或阈值提到 100%，防「CI 全绿与事实 FAIL 并存」反复

## 八、第四轮复验裁定（2026-08-14，蓝队窄修 0afcb0f+2b222a6，master=d9c818e）

> 红队按第四轮窄修 3 项逐项复验（git archive HEAD 为准，排除未提交内容）。

### 裁定：3/3 闭环，附 1 项文档一致性改进（P2）

| R | 裁定 | 依据 |
|---|------|------|
| R2-04 | ✅ 闭环 | 根+模板 `_meta.yaml:495-496` 均钉版（ruff==0.15.20 pyyaml==6.0.3 / import-linter==2.13），两份 meta 字节一致；根/模板 workflow 字节一致 |
| R2-11 | ✅ 闭环 | rev-sha projection 事实（check_consistency.py:314-315 `all(f'rev: {sha}' ...)` 覆盖三仓库）；精确变异三 SHA 全部 rc=1 硬失败 + `[FAIL] rev-sha projection`；root/scaffold 三 rev 与真源逐项一致；render_meta --check 变异后 rc=2 漂移检出、重渲染一致 |
| R2-10 | ✅ 闭环（裁定口径） | 蓝队选择「边界文档化」路径：SECURITY.md「SKIP 环境变量使用边界」节四要素齐全（允许/禁止/审计/补跑）+ 两条使用记录（08ee9f9、5804090）真实落账（worklog 同源）；红队实证：CI 全文 0 处 SKIP 引用、gitleaks 无条件执行（sha256 校验）；补跑承诺诚实（本地 golangenv 编译确实受限，实跑 100s 卡编译）。裁定：SECURITY.md 作为安全策略文档是边界规则的合理归属（豁免账只登记 [skip-*] 破例，SKIP 系运行环境降级，两者独立），第二选项成立 → 闭环 |

### 改进建议（P2，非阻塞，纯文档一致性）

- **SKIP 边界规则「孤岛」**：边界仅存于 SECURITY.md；`meta/豁免清单.md` §一（豁免审计唯一权威）与 08-汇报收束 均无交叉引用——执行者只看豁免账会误以为「破例只有 [skip-*] 一种」。建议豁免清单 §一 规则区加一行交叉引用（SKIP 环境变量使用边界见 SECURITY.md 对应节）或 FILE_GRAPH 注释同步

### 累计战绩（截至第 2 天）

- 红队累计发现 **24 项**（R-01~21 + R2-09/10/11），**全部闭环**；剩 1 项 P2 文档交叉引用改进建议待蓝队顺手补齐
- 红队连续 4 轮拦截/发现模式：无效 SHA（在途）、真源浮动（R2-04）、阈值豁免假绿（R-02/R2-09/R2-11）、破例未登记（R2-10）——系统性根因（聚合阈值豁免）已两度入技术债建议

## 九、S-1 攻击裁定（2026-08-14，蓝队实现 993f450，master=9d29032）

> 红队按 7 条验收矩阵攻击（git archive 只读 + 变异 + 注入 + 双线程）。

### 裁定：**实现 ✅ 可验收；测试证据链 ❌（P2，vacuous）；文档 ❌（P2，3 项）**

| 验收项 | 裁定 | 依据 |
|--------|------|------|
| ① 写入注入 | ⚠️ 实现过/测试虚 | 红队注入 `_atomic_write` ENOSPC/EACCES、目录 rename ENOSPC/EACCES → ScaffoldError + target/staging/tmp 零残留 ✅；真实 main() 第 3 写失败 rc=1、calls=3、零残留 ✅；但**蓝队新增测试无法证明 staging 机制**（见下） |
| ② 事务语义 | ✅ | 空 target：target 同级 `.devguard-staging-<token_hex>` 构建 → verify+receipt hash → 单次 os.replace（同一父目录，同文件系统）；失败 rmtree 归零；spy 确认 1 次目录 rename |
| ③ owner 保护 | ✅ | --force 非空 target 走既有回滚路径（staging=None），owner 既有文件备份/恢复语义保留；且该路径不经 os.replace，规避 Windows rename 风险（设计自洽） |
| ④ 原子性 | ✅ | 单次 os.replace 提交；提交前无部分写入可见；rename 失败注入零残留；成功路径无 staging/tmp 残骸 |
| ⑤ Windows 真机 | ✅ | test_scaffold.py 未被 cross-platform job 排除 → 两新测试在 windows-latest 实跑；os.replace 空 target 场景真机验证；monkeypatch 注入点平台无关 |
| ⑥ ENOSPC/EACCES | ⚠️ 实现过/测试缺 | 红队注入验证行为正确（零残留）；蓝队自认待补测试——**建议补入 test_scaffold 锁行为** |
| ⑦ 回归 | ✅ | 213/213 全绿；consistency 27/27；既有 D1-D6 测试无回归 |

### 新发现：S1-A4（P2）· 新增 staging 测试 vacuous

- **证据**：`test_staging_cleaned_on_failure_when_target_empty`（test_scaffold.py:480）与 `test_staging_atomic_commit_leaves_no_residue`（:506）均直接调 `module.setup`（非 main），不 spy os.replace、不断言 calls；**决定性变异：临时把实现改为「empty_target 直接 work_root=target、完全不建 staging」→ 两测试仍全绿（2 passed）**——对 staging 机制零敏感，回归时无法拦截 staging 损坏
- **期望**：测试须能证明 staging 生效（变异实现必 FAIL 的哨兵）：改走 main 路径，或加 `assert calls==3` + spy 目录 rename/路径断言 + receipt/hash 全量断言
- **注**：实现本身经红队多路注入验证正确——本项为「证据链缺口」非「功能缺陷」，但蓝队验收声称「新增 2 测试」属同款「声称与事实不符」模式（测试存在但测的不是 staging）

### S1-A3 残留（P2，3 项）

1. 方案稿 a 点「非空 target 保留回滚清单」vs c 点「--force：payload 仍在 staging 构建」自相矛盾未澄清（实现取 a，c 成纸面）
2. S-1 方案稿未登记索引（design/INDEX、README-设计文档说明、FILE_GRAPH §一 均无）——违反 FILE_GRAPH §四.3
3. README L101 仍写「逐文件原子替换+回滚」旧语义——空 target 新语义（staging+os.replace）未更新，D1 文档与实际行为脱节

### S-1 窄修清单（4 项）

1. 重写 2 个新测试为 staging 敏感（main 路径或 spy+哨兵变异），补 ENOSPC/EACCES 注入测试
2. 方案稿 a/c 矛盾澄清（c 点改为「--force 保持回滚路径」或删除）
3. S-1 方案稿登记索引（design/INDEX + README-设计文档说明 + FILE_GRAPH §一）
4. README L101 事务语义更新（空 target=staging 原子提交；--force=回滚清单）

## 十、第五轮主动攻击发现（2026-08-14，蓝队修复窗口期）

> 红队不等修复、主动连续攻击。快照 master=9d29032。以下两条为红队队长现场实测复现。

### R2-12 [P1] 新项目钩子覆盖缺口：PRD 承诺「19 钩子」vs 实际 12 个，7 个治理闸门缺席

- **证据**：新项目（`setup_scaffold --install` 实测）钩子 = 10 pre-commit（check-added-large-files/check-json/check-yaml/end-of-file-fixer/trailing-whitespace/ruff/ruff-format/gitleaks/devguard-verify）+ commit-msg 2 个（gitleaks + conventional-commit）；根仓 19 = 10 pre-commit + 9 commit-msg 治理闸门（commitlint/worklog-ref/status-updated/worklog-structure/file-placement/exemption-log/updated-tag/doc-sync/convergence-gate）
- **PRD 声称**：L101「初始化完成：…19 钩子…」；L127「pre-commit 19 钩子 格式/安全/一致性/文件放置/豁免/同步」
- **实测**（/tmp 真机）：新项目 `feat: no worklog` 无任何 worklog 文件提交成功 rc=0——根仓必拦的 worklog-ref 闸门在新项目完全缺席；`not conventional` 被拦 ✅（conventional-commit 生效）
- **影响**：新项目从第一天起缺失 worklog/STATUS/文件放置/豁免/同步 7 项治理强制——「机器拦截」卖点在初始化项目上只剩格式+密钥+规范提交；若为有意设计（新项目无 worklog 历史，ref 闸门会拦死首个提交），PRD 必须修正声称；若为疏漏，需补钩子并解决「首个 worklog 前提交死锁」
- **修复建议**：二选一——(a) PRD/README 修正为「12 钩子（新项目）+ 治理闸门随项目成长启用」；(b) 补全 commit-msg 闸门并设计引导期豁免（如首个提交仅要求建立 worklog）

### R2-13 [P2] project_name 输入校验缺失：JSON 破坏致初始化 DoS、AI 入口文件可被注入

- **证据**：`_render_text`（setup_scaffold.py:139-141）无转义直接 replace 进 8 个文件（.devguard.json/CLAUDE.md/AGENTS.md/README.md/STATUS.md/SKILL.md/开发清单.md/背景.md）；`--project-name 'X"}]},{broken'` 实测 → 初始化失败（devguard.json 语法破坏）且回滚后 target 零残留 ✅（D3 语义在此路径成立）
- **意外防线**：注入被拦**仅因** .devguard.json 的 JSON 语法校验——非显式输入校验；换行/引号类载荷被 JSON 校验意外拦截，但 markdown 特殊字符（**、`、链接）可原样进入 CLAUDE.md/AGENTS.md（AI 入口文件）
- **影响**：P2——输入校验缺失 + 防线系「意外」而非设计；自动化场景（CI/脚本以不可信名初始化）可 DoS；AI 入口文件可被同构造的 markdown 污染
- **修复建议**：显式校验 project_name（禁控制字符/引号/换行，或白名单 `[\w\- ]`）；`_render_text` 按目标格式转义（JSON escape / markdown escape）

### R5-01 [P1] receipt/verify 完整性语义缺口：`--verify` 对篡改文件零检出

- **证据**（成员 019ff62d 实跑）：setup 后改 target/README.md 为 `CORRUPTED PAYLOAD`，receipt 内 sha256 改为合法全 0 的 64hex → `module.verify()`=[]、`--verify` rc=0、生成项目 `devguard.py verify` rc=0
- **根因**：`verify()`（setup_scaffold.py:402）→ `_validate_receipt(check_hashes=False)`（:418）只校验语法；receipt 哈希比对仅存在于 setup 内部（:364 check_hashes=True）；生成项目的 devguard.py 不比对 receipt hash；现有测试只覆盖删除/不完整 receipt，不覆盖「文件被修改」
- **影响**：D5「复验既有目标」对篡改文件零保证——若 D5 语义为完整性则为 P1 缺陷；若 receipt 定位为 provenance（来源记录）非 integrity，须在 PRD/文档显式澄清（当前无任何文档说明此边界）
- **修复建议**：二选一——(a) `--verify` 增加可选 `--check-integrity` 全量哈希比对；(b) PRD D5 与 receipt 文档明确「provenance 非 integrity」语义

### R5-02 [P1] 钩子可执行位不校验：`--require-hooks` 声称在场，实则可静默失效

- **证据**（成员 019ff62d 实跑）：生成 target 后创建 `.git/hooks/pre-commit`/`commit-msg` 内容 `exit 1` 但 chmod 0644，配置 local core.hooksPath → `module.verify(require_hooks=True)`=[]、生成 `devguard.py verify --require-hooks` rc=0；实际 `git commit -m 'bad nonconventional'` rc=0（git 对不可执行钩子报 ignored）
- **根因**：`hook_exists` 仅检查 is_file()/.exe，不校验 POSIX 可执行位（Windows 按平台语义）
- **影响**：钩子可被 chmod 静默中和而 verify 仍报「在场」——C2/D5 声称与事实不符；恶意者或误操作（备份还原、压缩解包丢执行位）均可制造此态
- **修复建议**：require_hooks 校验可执行位（POSIX os.access X_OK；Windows 按平台）；verify 输出含钩子清单与状态；补「非可执行钩子 → verify FAIL」测试

### A5-01 [P1] .gitleaks.toml allowlist 零治理（C5 单点旁路）

- **证据**（成员 019ff62f）：allowlist 是 C5 密钥拦截的单点旁路；scripts/ 与豁免账全文 0 处 allowlist 引用；无变更提示钩子/审计登记——修改 allowlist = 给任意路径开密钥白名单，普通 PR 即可合入且无痕迹
- **修复建议**：pre-commit 检测 .gitleaks.toml 变更时提示人工确认（或强制登记审计日志）

### A5-02 [P2] llms.txt 事实漂移 3 处

- llms.txt L10 声称「AGENTS.md：Codex 项目入口」——**根目录无 AGENTS.md**（仅 scaffold 模板 .tmpl）；「check_*.py（23 个）」实测 **24 个**（check_coverage_matrix 后未更新）；内容未反映 R2 轮次与 S-1（过时非错误）

### A5-03 [P2] commitlint 语义边界（记录级）

- `feat(core): adds secret key logging to stdout` / `fix(API): fix bug` / `feat!: breaking` 均 rc=0——commitlint 校验格式不校验语义，属工具固有边界；建议 03-git 规范注明「commitlint 不替代人工语义审查」

### 通过项

- **dashboard 渲染幂等 ✅**（成员 019ff62f）：render_mtime 恒定默认值，连续两次渲染 diff 为空，S7 无漂移风险

### R5-03 [P2] install_hooks 失败不事务化：可破坏既有 core.hooksPath

- **证据**（成员 019ff62d 实跑）：用无 pre_commit 的 system Python 调 standalone installer → 先执行 `_reset_composed_hooks` + `_unset_local_hooks_path` 再检测安装结果；预设 local `core.hooksPath=external-hooks` 后失败 → 前值 external、后值 `<unset>`（**既有配置丢失**），且留下 `.git/devguard-empty-gitconfig` 残留
- **影响**：失败不事务化——重新安装/降级场景可静默清除用户既有 hooksPath 配置（与 D3 事务语义同类问题，但作用于配置而非文件）
- **修复建议**：变更前 preflight `sys.executable -m pre_commit --version`；或失败时恢复原 config 值；补「install 失败 → hooksPath 原值保留」测试

### R5-04 [P2] pre-push 覆盖缺口：新项目无自有 pre-push 门禁

- **证据**（成员 019ff62d）：install_hooks 不创建 DevGuard 自有 pre-push；无 external pre-push 时 `.git/hooks/pre-push` 不存在；有 external 时仅 exec external（`pre-push wrapper 仅 exec external/pre-push`），devguard verify/pytest 不在 pre-push 链
- **影响**：若 PRD 对 pre-push 有门禁预期（push 前全量验证）则为覆盖缺口；若设计意图为「保留既有 external hook」则 wrapper 的 set -e 失败传播成立——需在文档澄清设计意图
- **修复建议**：明确 pre-push 设计语义（devguard 门禁 or 仅透传 external）；如需门禁，链式执行 external + devguard verify

### R5-05 [P2] install_hooks 只看子命令 rc 不验 postcondition：可假成功

- **证据**（成员 019ff62d）：临时 fake `pre_commit` module（`python -m pre_commit install` 返回 0 但不写 hook）跑 standalone install_hooks.py → rc=0、打印 HOOK INSTALL OK，但 `.git/hooks/pre-commit`/`commit-msg` 均不存在；后续 `devguard.py verify --require-hooks` rc=1
- **影响**：standalone 文档命令可假成功（「声称 OK 实为无钩子」）；`setup --install` 的后置 verify 可兜底，但独立使用路径无保护
- **修复建议**：install_hooks 安装后校验 postcondition（hook 文件存在+可执行），失败即 rc=1

### R5-06 [P2] devguard-verify 不验 hook entry 语义：本地闸门可被配置损坏变 no-op

- **证据**（成员 019ff62d）：setup 后把 scaffold `.pre-commit-config.yaml` 的 local hook `entry: python scripts/devguard.py verify` 改为 `entry: python -c "pass"`（保留 id/name/stages 等 required 子串），不改 receipt → generated `devguard.py verify --root` rc=0、`setup_scaffold --verify` rc=0
- **影响**：verify 只做词条闭合（substring 级），不验证 hook entry 语义/receipt digest——配置损坏可把本地闸门变 no-op 后验收仍绿；与 R5-01 receipt 不比对同族（若「owner 可编辑契约」明确不要求完整性则需文档化）
- **修复建议**：verify 校验 hook entry 指向真实存在的 devguard.py/可执行路径；或文档明确边界

### R5-07 [P2] install_hooks 外部钩子路由：global 优先，local 既有链可被丢
- **证据**（成员 019ff62d 实跑）：`_effective_external_hooks_path` 优先候选 global_hooks，再 stored/effective local。实测机器已有 global `core.hooksPath=/Users/xbpd/.codex/git-hooks`、目标另设 local `core.hooksPath=/tmp/external-hooks` → 安装后 wrapper 链的是 global 路径，local external 未链/未登记，随后 local core.hooksPath 被改为 `.git/hooks`
- **补充证据（同族）**：隔离 global 后、target 默认 `.git/hooks` 已有 owner 自建 `pre-commit`（exit 23，非 composed marker）+ `pre-push` → install_hooks rc=0 后 pre-commit 被 pre_commit install **直接覆盖**（OWNER-LOCAL 消失），pre-push 保留——`_effective_external_hooks_path` 将 effective==local 视为无 external，不备份/串联既有 local pre-commit
- **影响**：若契约要求保留「既有 effective hook policy」（含默认 local hooks）则为丢钩链（P1/P2）；若明确 global 优先则需文档说明（当前无任何文档）；隔离 global 后 local-only 场景链路正常
- **修复建议**：文档化路由优先级（global vs local）；或改为 local 优先并登记 external 链；安装前备份既有 local pre-commit（composed marker 判定需覆盖 owner 自建场景）

### 第五轮累计（蓝队修复窗口期主动攻击）

**P1×4 / P2×10 / P3×1**：R2-12 新项目钩子覆盖缺口 / R5-01 receipt 完整性语义 / R5-02 钩子可执行位 / A5-01 allowlist 零治理（P1）；R2-13 project_name 注入 / A5-02 llms.txt 漂移 / A5-03 commitlint 边界 / S1-A4 vacuous 测试 / S1-A3 文档 3 项 / R5-03 install_hooks 非事务化 / R5-04 pre-push 缺口 / R5-05 install_hooks 假成功 / R5-06 devguard-verify 词条闭合 / R5-07 外部钩子路由 global 优先（P2）；R5-08 02 红线可检测性标注缺失（P3）

### R5-08 [P3] 02 规范红线可检测性未标注（记录级）

- 约束覆盖率矩阵自认 02 的 L1=ruff+gitleaks+markdownlint，最后人工列「👤 日志脱敏抽查」——「禁 print/SQL 参数化」等红线无机器检测，矩阵如实标注（非声称失实）；但 02 规范红线表未标注可检测性，读者易误以为机器拦截
- 建议：02 红线表增加可检测性标注（机器/人工），与矩阵口径对齐

### 通过项与自查记录

- worklog-ref 存在性校验 ✅（红队自查纠错：`_staged_blob_exists` 校验引用真实在暂存区，非仅格式）
- dashboard 同步纪律 ✅（dashboard.html 与 STATUS.md 同 commit 更新）
- dashboard 渲染幂等 ✅

---

**红队队长（019ff632）汇总签发 ｜ 成员 019ff62f / 019ff62d 协作 ｜ 2026-08-14**
