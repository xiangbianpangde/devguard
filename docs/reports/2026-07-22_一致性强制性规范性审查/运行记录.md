# 运行记录：2026-07-22 审查实跑证据

> 更新: 2026-07-22
> 环境: macOS（darwin），仓库 `.venv`（pytest 9.1.1），工作树含未提交的 #55 修改

---

## 1. pytest 全量（两次）

```bash
# 第一次：未激活 venv（.venv/bin 不在 PATH）
.venv/bin/python -m pytest -q
# → 1 failed, 203 passed in 5.67s
# FAILED tests/conventions/test_02_coding.py::TestCodingContracts::test_ruff_check_runs_clean_on_src_coding
#   FileNotFoundError: 'ruff'（测试以裸命令调 ruff，依赖 PATH）

# 第二次：PATH 含 .venv/bin
PATH="/Volumes/项目/devguard/.venv/bin:$PATH" .venv/bin/python -m pytest -q
# → 204 passed in 6.23s
```

结论：STATUS「204 tests passed」属实，但 test_ruff 用例对环境 PATH 敏感（F17）。

## 2. 一致性事实矩阵

```bash
.venv/bin/python scripts/check_consistency.py
# 进度真源: 3/5
#   [FAIL] STATUS详细表 continuity: rows=54/54 missing=[54] extra=[55]
#   [FAIL] 开发清单详细表 continuity: rows=54/54 missing=[54] extra=[55]
# 规范-BDD契约: 7/7 全 PASS
# 强制闸门绑定: 8/8 全 PASS
# CI模板投影: 2/2 全 PASS（workflow == 模板逐字节；ruff 0.15.20 四方一致）
# 元数据可复现: 1/1 PASS
# 故障注入拦截: 1/1 PASS
# 总计: 22/24 = 91.7% (阈值 80.0%) → OK 一致性达标
```

## 3. 故障注入矩阵

```bash
.venv/bin/python scripts/check_enforcement.py
# 10 项注入全部 [BLOCKED]：missing-worklog-reference / phantom-worklog-reference /
# unaudited-worklog-skip / malformed-worklog / worklog-status-desync /
# root-file-placement / unregistered-exemption / stale-update-tag /
# status-plan-structure-drift / unconverged-node-crossing
# 强制性拦截率: 10/10 = 100.0% (阈值 90.0%) → OK 强制性达标
```

## 4. git 考古关键输出

```bash
git ls-files scripts/ tests/conventions/ | grep -i install   # → 空（F01）
git status -s --untracked-files=normal
#  M CLAUDE.md / README.md / STATUS.md / docs/plan/开发清单.md / docs/reports/INDEX.md / meta/FILE_GRAPH.md
# ?? scripts/install.sh, scripts/install.ps1, tests/conventions/test_installer.py
# ?? docs/reports/2026-07-21_AI_four_pillars_from_scratch.html
# ?? docs/reports/2026-07-21_devguard_template_full_trigger_demo.html

git show HEAD:STATUS.md | head -4
# > 更新: 2026-07-17（审查加固…；#53 final-report…）
# <!-- devguard-progress: completed=53 total=53 -->

# 豁免账 §三 行数按提交追溯（F03）
# 0860261: 2 行 → d1f2bfd: 2 → db94313: 2 → dd8e651: 2 → eaad917: 2 → c3ecd55: 0 → HEAD: 0
# 抹除者 = c3ecd55 "feat(governance): harden measurable gates and scaffold"

git log --all --oneline -- scripts/install.sh   # → 空（install.sh 从未入库）

git log --all --grep='skip-' --oneline
# 0860261 feat(devguard): V2.3 #52 收束硬闸门… [skip-gate] [meta]（历史唯一实际使用）

ls worklogs/ | grep -iE "fp55|install|安装"   # → 空（F01 无 #55 worklog）
```

## 5. 静态对账关键输出

- `_meta.yaml` conventions 条目 = 17（README.md:107 写「16 规范元数据」→ F10）
- `.pre-commit-config.yaml` hooks = 19；`ci.yml` jobs = 5；`worklogs/decisions/` = 8
- `tests/conventions/` 33 文件；`def test_` 静态计数 194（+parametrize = pytest 收集 204）；`test_perf_baseline.py` = 0（F19）
- ci.yml L44 gitleaks 下载 `v8.24.3`；.pre-commit-config.yaml L33 gitleaks `v8.18.0`（F13）
- `docs/specs/` 12 份：00-07、08-code-understanding、08-examples、09、10（F12）
- `docs/templates/` 18 文件（13 md + 5 html），索引登记 17 行；未登记：计划/实施设计/绘图素材库/汇报 4 个 .html（F14）
- `git ls-files "docs/历史文件/"` 有记录（tracked），FILE_GRAPH 目录树未含该目录（F15）
- STATUS.md:5 `convergence-gate: nodes=31,35,46,49,52 last_converged_fp=52`（F02）
- STATUS.md 收束历史：v0.2–v2.2 共 16 行「人审计 ⏳ 待签核」，仅 v0.1 已签核（F07）
- dashboard.html 含「188」×2（与 HEAD 已提交 188 测试口径一致；工作树 204 = 188 + 未入库的 16 个 installer 测试）
- CLAUDE.md:33「scripts/ # start_server.py / 打开仪表盘.bat」；:46「docs/specs/ (01-08)」；:129「53/53 ✅」（F08/F09/F12）
- README.md:26「V2.2.0」 vs :143 页脚「V2.0.1」；STATUS.md:22-23 阶段名「V2.4 增强」；CHANGELOG.md 最新 `[V2.2.0]`，Unreleased 仅 #53，无 #55（F05）
- README.md:34-37（manifest 初始化）vs :145-158（`cp -r 开发规范/` 快速开始）（F06）
- ci.yml L165/L169 注释「V2.3 #48」「V2.3 #50」与 STATUS 主表 #48/#50 不同义（F11）
