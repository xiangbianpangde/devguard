# 决策登记：语义修复包 B（2026-07-22）

> 更新: 2026-07-22
> 授权: master 批示「你做决策，但是要进行登记」（2026-07-22）
> 决策人: 小袁（WorkBuddy）；登记方式: 本文件 + STATUS 阻塞项 + worklog
> 边界: 「人触发收束」「人审计签核」属 Owner 专属动作，AI 不代办（`08-汇报收束.md`），仅登记待办并武装闸门。

---

## D1 · #55 处置（B1）→ 补登入库，不回退

**决策**: 采用选项①——补写 worklog、真机复验后按本仓流程提交 `install.sh` / `install.ps1` / `test_installer.py` 及四件套修改。
**理由**: 产物真实存在且 16 个 installer 测试全绿；回退（选项②）等于销毁已完成工作并伪造历史。欠的是「入库 + 汇报」闭环，不是产物本身。
**证据**: 2026-07-22 真机复验——`install.sh --dry-run` 干跑通过；`/tmp/devguard-e2e-fp55` 全量安装 + `--verify --require-hooks` 复验（结果见 `worklogs/2026-07-22_v2.4-fp55-one-command-installer.md`）。
**代价声明**: worklog 为 2026-07-22 补登（交付日 2026-07-17），在 worklog 中显式标注补登原因，不掩盖断档事实。

## D2 · #54 跳号（B2）→ 主表补行立项

**决策**: 采用选项①——STATUS / 开发清单 / CLAUDE 三处补 `#54 merged_report 存量迁移（final-report 契约全覆盖）`（⏳ 待开始），`total` 54→55，`completed` 维持 54。
**理由**: CHANGELOG [V2.3.0] 已把 merged_report 迁移工作指给 #54，补行使「编号占用」成为明文事实；check_consistency 两项 continuity FAIL（missing=[54]）随之消除，一致性矩阵回到 24/24。选项②（改检查容忍跳号）会弱化真源纪律，不取。
**连带**: V2.3 子计划历史编号与主表 #54 的撞号，在 ci.yml 注释（A5 已做）与本登记中双重澄清。

## D3 · 收束再武装（B3）→ 续写节点；收束与签核登记为 Owner 待办

**决策**: STATUS 闸门标记续写为 `nodes=31,35,46,49,52,55,58`（`last_converged_fp=52` 不变），恢复「默认每 3 功能点」的节点节奏；以下两项登记进 STATUS 阻塞项，**AI 不代办**：
1. **v2.3 收束**（覆盖 #53/#55 四阶段）——等 Owner 触发；未收束前 #54/#56 的交付提交将被收束闸门拦截（fail-closed 生效）。
2. **v0.2–v2.2 共 16 节点人审计签核**——等 Owner 批量签核（2026-06-30 目标已逾期，不计完成）。
**理由**: 闸门断供的根因是「节点表要人续写而无人续写」；续写后机器恢复拦截能力，但「触发收束」「签核」按规范仍属人——这是本项目自己划的界线（`08-汇报收束.md`：AI 不替人做收束）。

## D4 · 版本号治理（B4 + F06）→ 补发 V2.3.0 / V2.4.0

**决策**:
- `[V2.3.0] - 2026-07-17`：#53 final-report 入族 + 当日 4 项审查加固修复（markdownlint POSIX 空转 / render_meta 防截断 / ruff 真源 / 脚本镜像）——均已在当日提交，按交付日落版。
- `[V2.4.0] - 2026-07-22`：#55 单命令跨平台安装器 + 2026-07-22 审查 A/B 修复包。
- README 三处口径统一为 V2.4.0（L26 / L106 / 页脚 L143）；CLAUDE 治理基线 V2.2.0→V2.4.0。
- **F06 并入本项**：README「快速开始」的 `cp -r 开发规范/` 教学删除，改为指向 install.sh / setup_scaffold（manifest 初始化），消除与 L34-37 的自相矛盾。
**理由**: semver 按交付事件补发；`cp -r` 会夹带 node_modules/.venv/worklogs 等本仓资产，与脚手架设计直接冲突，属文档级红线误导。

## D5 · 豁免账（B5）→ 恢复记录 + 立项硬闸门（登记待办）

**决策**: 按 `0860261` 版本恢复 §三 2 条使用记录（`[skip-updated]`、`[skip-gate]`，2026-06-11），并加恢复注记（抹除者 c3ecd55、恢复日 2026-07-22）；「§三 记录只增不删」硬闸门检查**登记为待立项**（进 STATUS 阻塞项），不在本轮直接改 `check_exemption_log.py`——新增闸门属功能点，应走「STATUS + 开发清单加行 + BDD」流程，不在审查修复权限内自行扩编。
**理由**: 恢复审计轨迹是本包职责；造新闸门是规划动作，越权自创流程恰是本审查点名的反模式。

## D6 · 07/09/10 file 锚点（B6）→ 接受目录锚点并写入 schema 注释

**决策**: 不改结构，在 `_meta.yaml` 第 1 节头部加注释，显式声明「目录型规范：07/09/10 的 file 指向 ai-workflow 目录（规范族锚点），render_meta 对其 SKIP 分级渲染」为既定 schema 行为。
**理由**: 三条规范共享流程族目录是架构现实（ADR 0005 长文拆目录），低成本声明优于强行拆文件。

## D7 · 本机 hooks 旁路（新发现 F20）→ 解除本仓 core.hooksPath

**决策**: `git config --unset core.hooksPath`（本仓 local 级），恢复 `.git/hooks/` 生效。
**事实**: 本仓 local `core.hooksPath=/Users/xbpd/.codex/git-hooks`（ECC Codex 全局 hooks，仅 pre-commit + pre-push，**无 commit-msg**）→ 本仓 pre-commit 框架安装的 19 个钩子（含 8 个 commit-msg 治理闸门）在本机**全部不触发**；README 声称的「串联既有 hooks」未发生。
**理由**: unset 后本仓 19 钩恢复（含 gitleaks，覆盖 ECC 密钥扫描职能）；不改 ECC 共享目录（影响面跨仓）。如需 ECC 扫描并存，可后续跑 `docs/templates/devguard/scaffold/core/scripts/install_hooks.py` 做正规串联。
**登记**: F20 补入审查发现清单（强制性，P0 级——本机闸门空转）。

---

## 执行顺序（依决策）

1. B1 证据：install.sh dry-run + 全量安装 + verify（真机，本文件登记时已在跑）
2. 文档编辑：B2 / B3 / B4 / B5 / B6 / F06
3. worklog ×2（#55 补登 + 今日审查修复）
4. 回归：pytest / check_consistency（预期 24/24）/ check_enforcement / render --check / 各 check_*.py
5. 提交（本仓纪律，不 push）：C1 = #55 交付；C2 = 审查修复与治理决策
6. 修复记录.md 增补 + memory
