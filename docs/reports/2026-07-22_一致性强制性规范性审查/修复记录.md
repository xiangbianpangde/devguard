# 修复记录：机械修复包 A（2026-07-22）

> 更新: 2026-07-22
> 依据: 同目录 `审查报告.md` §五，master 批示「A」（仅机械包；语义包 B1-B6 待拍板）
> 执行人: 小袁（WorkBuddy）

---

## 一、逐项执行记录

| 项 | 动作 | 文件 | 结果 |
|---|------|------|------|
| A1 | 「16 规范元数据」→「17」；头部双日期行去重并刷新为 2026-07-22 | `README.md` | ✅ |
| A2 | L33 scripts/ 描述重写（19 闸门脚本/渲染/初始化）；L46 specs 范围改「00-10 + 双 08 例外」；L129「53/53」→「54/54」并补 #55；头部日期刷新 | `CLAUDE.md` | ✅ |
| A3 | 补登 4 个 HTML 模板（计划/实施设计/汇报/绘图素材库）；更新记录加 v1.4 行；日期刷新 | `docs/templates/README-模板索引.md` | ✅ |
| A4 | 目录树补登 `docs/历史文件/` + specs 双 08 例外声明；技术债 #1 更正为「已移至 docs/历史文件/ 且 tracked」；日期刷新 | `meta/FILE_GRAPH.md`、`STATUS.md` | ✅ |
| A5 | ci.yml 头部加编号说明块；注释 `V2.1 #NN`/`V2.3 #NN` → `V2.x 子计划 #NN`（8 处） | `.github/workflows/ci.yml` + `docs/templates/devguard/.github/workflows/ci.yml`（镜像，diff 验证逐字节一致） | ✅ |
| A6 | specs 双 08 采用「显式声明例外」方案（不更名，零引用破坏），声明落入 FILE_GRAPH 与 CLAUDE.md | 见 A2/A4 | ✅ |
| A7 | `git checkout master`（与 feat/fp55 同点，工作树无损）+ `git branch -d feat/fp55-one-command-installer` | git 分支 | ✅ 本地已清；`feat/fp53` 本地不存在仅远端有，`origin/feat/*` 远端分支未动（删远端需另行批准） |
| A8 | `_meta.yaml` toolchain 新增 `gitleaks: 8.24.3` 真源；pre_commit rev v8.18.0→v8.24.3；`render_meta.py --render` 重渲染；双 ci.yml 加真源指引注释 | `conventions/_meta.yaml`、`.pre-commit-config.yaml`（渲染产物）、双 ci.yml | ✅ |
| A9 | 非空占位误判更正：补 `test_perf_baseline_script_loadable` smoke test；注释更新（53→200+ 口径、仅 warn 不 fail 的设计意图） | `tests/conventions/test_perf_baseline.py` | ✅ |
| A10 | 新增 `_ruff_executable()`：优先 `sys.executable` 同级目录（含 ruff.exe），退回 PATH，仍 fail-closed；替换裸命令调用 | `tests/conventions/test_02_coding.py` | ✅ |

## 二、回归验证（修复后实跑）

| 检查 | 修复前 | 修复后 |
|------|--------|--------|
| pytest（不带 venv PATH） | 1 failed / 203 passed（ruff 假失败） | **205 passed**（204 + A9 新增 1；A10 假失败消除） |
| `check_consistency.py` | 22/24 = 91.7% | 22/24 = 91.7%（剩余 2 项 FAIL = F04 连续性，属 B2 语义包，未批准不动） |
| `check_enforcement.py` | 10/10 = 100% | 10/10 = 100% |
| `render_meta.py --check` | PASS | PASS（A8 重渲染后 pre-commit 与真源一致） |
| 双 ci.yml 逐字节 | 一致 | 一致（镜像编辑后 diff 验证） |
| gitleaks 版本 | pre-commit 8.18.0 / CI 8.24.3 双钉 | **统一 8.24.3**，真源 `_meta.yaml toolchain.gitleaks` |

## 三、未动项（语义包 B，待 master 拍板）

B1 #55 入库处置（含 worklog 补写）/ B2 #54 跳号 / B3 收束再武装 + 人审计签核 / B4 版本号治理 / B5 豁免账恢复与防抹除 / B6 07-09-10 file 锚点。

**注意**：本批修改与 #55 未提交产物同在工作树。B1 落地前，STATUS/CLAUDE 的「54/54」仍为未提交声称——建议 B1 拍板后与本批修改一并按本仓流程提交。

## 四、附带说明

- 审查产出（本目录 4 份文件）亦未提交，提交时按本仓 commit-msg 纪律需引用 worklog。
- F06（README `cp -r` 与 manifest 矛盾）在报告中标「机械+」但未入 A 包清单，未动；如需处理可归入 B4 版本号治理时一并重写「快速开始」节。
