# 收束报告 v2.1：一致性、强制性与一键初始化

> 更新: 2026-07-11
> 范围: 功能点 #47-#49 | 状态: 本地四阶段完成，平台保护待认证回读

## 一、整理

本轮把历史 49/46/35 口径统一为 STATUS 真源、开发清单和 CLAUDE 同值投影；详细功能点连续编号 1-49。渲染器不再污染 LICENSE/CODEOWNERS，模板部署改为显式 core/optional manifest。

```mermaid
flowchart LR
    S[STATUS 真源] --> C[一致性事实矩阵]
    G[隔离 Git 违规] --> E[强制拦截矩阵]
    M[显式 manifest] --> I[一键 install]
    C --> CI[CI 必需检查]
    E --> CI
    I --> V[目标自身 verify]
```

## 二、测试

| 检查 | 结果 | 证据边界 |
|---|---:|---|
| 聚焦 P0/P1 测试 | 59/59 | 渲染、CI、8 gates、矩阵、脚手架 |
| 仓外 scaffold E2E | PASS | 真实联网安装、Git、双 hook、目标测试与复验 |
| pre-commit 全量 | PASS | ruff/format/gitleaks/markdownlint 等全部通过 |
| 全量 pytest | 128/128 PASS | 手工审查修订与回归测试补齐后的最终全仓回归 |

## 三、审计

| 门禁 | 实测 | 阈值 | 结论 |
|---|---:|---:|---|
| 一致性事实矩阵 | 24/24 = 100% | >=80% | PASS |
| 强制性故障注入 | 10/10 = 100% | >=90% | PASS |
| 合规语义扫描 | 66 pass / 0 fail | 0 fail | PASS |
| import-linter | 2 kept / 0 broken | 0 broken | PASS |
| Spectral | 0 error | 0 error | PASS |
| pip-audit | 0 known vulnerability | 0 | PASS |
| npm audit | 0 vulnerability（升级 markdownlint-cli 后） | 0 high | PASS |

## 四、效果验证

在仓库外空目录执行 optional profile 的完整 `--install`。结果为 20 个 manifest 文件生成、隔离 `.venv` 安装、pre-commit 与 commit-msg 安装、目标自检 `DEVGUARD OK`、目标 2 tests passed、全部 pre-commit 与 Conventional Commit gate 通过，最后由源脚本 `--verify --require-hooks` 再验并安全清理临时目录。

dashboard 已改为 Python 跨平台入口，读取 49 条编号明细；旧结果 `0 / 11` 已被测试锁定并修复。L4 卡片来自真实 pytest，不接受解析失败时的 `0/0` 回退。

## 五、技术债

| 项目 | 状态 | 影响 |
|---|---|---|
| GitHub `master` 保护规则 | 待外部认证 | Chrome 控制扩展不可用，内置浏览器未登录；尚不能回读平台配置 |
| 历史 v0.2-v1.5 人审计签核 | 14 个待 Owner | 不影响本轮机器门禁，但不能标记为人审通过 |

## 收束结论

本地一致性、强制性和易部署性目标均已达到并有重放证据；平台分支保护尚缺认证后的真实配置与回读，因此当前为**本地收束通过、项目级收束待平台闸门**。在该证据补齐前，不把 #49 或 v2.1 标记为全部完成。
