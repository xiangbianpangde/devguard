# 收束报告 — 收束节点 v0.3

> 收束时间: 2026-06-07
> 范围: V0.3 4 件任务（V3.1 / V3.2 / V3.3 / V3.4）

---

## 一、v0.2 vs v0.3 增量

| 维度 | v0.2 完成态 | v0.3 增量 | v0.3 完成态 |
|------|-------------|-----------|-------------|
| 防御层 L1（CI） | 9 钩子 pre-commit + 5 阶段 CI 验证 | markdownlint L1 加进 ci.yml L1 lint 阶段 | 10 钩子 pre-commit + 5 阶段 CI + 3 tool L1（ruff + gitleaks + markdownlint） |
| gitleaks cache | gitleaks-action@v2 带 cache drift 误报 | 改手动跑 gitleaks 8.24.3 binary，绕开 cache | 0 drift 误报 |
| dashboard.html | 手写硬编码 + 32/33 错 | render.py 2 bug 修复 + 整体切换 + CI 渲染步启用 | 100% (35/35) + 32 红线 + 模板渲染 |
| _meta.yaml l1_check | `ruff + gitleaks` | 扩展为 `ruff + gitleaks + markdownlint-cli` | L1 检测配置完整 |
| L4 测试 | 53 passed | 改 test_markdownlint_in_precommit_optional → _enabled（强制钩子存在） | 53 passed |

## 二、4 件任务交付明细

### V3.1 markdownlint 钩子启用（done）

- ✅ 新增 `.markdownlint.json` 配置（禁用 MD037/001/056/051/035/030/029/058/047 等软规则——中文常见误报）
- ✅ 新增 `.markdownlintignore`（cli 兜底排除非 .md）
- ✅ 新增 `scripts/lint_markdown.py` 包装脚本（Python 预过滤 .md，绕开 markdownlint-cli glob 不被尊重的 bug）
- ✅ `_meta.yaml` 取消 markdownlint 注释 + 改 entry 为 `python scripts/lint_markdown.py`
- ✅ `06-documentation l1_check` 改为 `markdownlint-cli + .markdownlint.json`
- ✅ 模板版 `docs/templates/devguard/` 同步 `.markdownlint.json` + `.markdownlintignore` + `scripts/lint_markdown.py`
- ✅ npm 装 `markdownlint-cli`（71 packages）
- ✅ 113 个 .md 全过 0 error，10 钩子全绿
- 提交链：`b4223ae`

### V3.2 render.py 数据 bug 修复 + dashboard 整体切换（done）

- ✅ 修 `docs/templates/devguard/html-report-template/render.py` `parse_status` 2 个 bug：
  1. `"BDD" in line` 误跳数据行（line 17 "统一模板 + BDD 规格" 被误跳）——改 `cols[0] == "功能点"` 精确匹配
  2. 跨节表格错收（"收束节点历史" 表格被收进"当前进度"）——加 `in_progress_section` 状态跟踪
- ✅ 重渲染 `dashboard.html`：100% (35/35) + 32 红线
- ✅ 启用 `ci.yml` build 阶段 Render + drift check 步
- ✅ `render.py` 写文件强制 `newline="\n"`（跨平台 LF 一致，CI Linux 不再因 CRLF drift）
- ✅ `render_date` 字段从 `datetime.now()` 改为 static `"latest"`（彻底幂等——同 commit 多次 render 0 drift）
- 提交链：`61d3efa` / `6fdd788` / `8226c2d` / `8d89b65`（V3.2 期间修复多次）

### V3.3 gitleaks cache disable（done）

- ✅ 改 `ci.yml` L1 lint 阶段 gitleaks 步：手动跑 gitleaks 8.24.3 binary（curl + tar + detect），不依赖 `gitleaks/gitleaks-action@v2` cache
- ✅ 不再触发 v2 cache drift 误报
- ✅ 实测 CI `gitleaks manual run V3.3` step 稳定 success
- 提交链：`06dcb74`

### V3.4 _meta.yaml l1_check 扩展 + L4 测试修补（done）

- ✅ `_meta.yaml` 02-coding `l1_check` 字段扩展为 `ruff + gitleaks + markdownlint-cli`
- ✅ `_meta.yaml` lint stage `tools` 注释更新（markdownlint 启用说明）
- ✅ `ci.yml` L1 lint 阶段加 markdownlint 步（`npm ci` + `python scripts/lint_markdown.py`）
- ✅ L4 `test_06_documentation.py` 改 `test_markdownlint_in_precommit_optional` → `_enabled`（强制钩子存在）
- ✅ L4 53 passed
- 提交链：`06dcb74`

## 三、CI 验证（V0.3 期间多次重跑）

| Run ID | Commit | 5 阶段 | 备注 |
|--------|--------|--------|------|
| `27091424059` | `06dcb74` | failure (L1 markdownlint) | CI Linux 找 `.cmd` shim 失败 |
| `27091473981` | `6318fd2` | failure (build dashboard drift) | render_date datetime.now() 每次跑不同 |
| `27091675753` | `6fdd788` | failure (build Render yaml ModuleNotFound) | CI build 阶段没装 pyyaml |
| `27091731858` | `9e7070e` | failure (build drift) | render_date commit time HEAD 变变 |
| `27091852327` | `9868e78` | failure (build drift) | render_date 用 git SHA 仍变 |
| `27091973665` | `8226c2d` | failure (build drift) | render.py 缺 import subprocess / 没用 |
| **`27092082957`** | **`8d89b65`** | **success** | V0.3 收尾首绿（render_date='latest' + 写 LF） |

- 7 次 push，6 次 fail，1 次 success
- 6 次 fail 分类：1 次 markdownlint .cmd shim（V3.4 跨平台）+ 1 次缺 pyyaml（build 阶段）+ 4 次 dashboard drift（render 幂等性反复调试）

## 四、技术债 / 遗留

| # | 项目 | 收束节点 | 备注 |
|---|------|----------|------|
| 1 | V2.2 计划 (l1_check 改 'markdownlint-cli' 替换 markdownlint 全文) | V0.4 | V0.3 已部分完成 |
| 2 | L1 检测配置扩展（其他规范加 l1_check） | V0.4 | 02-coding 已扩，其他 5 篇待 |
| 3 | dashboard.html 模板字段（csp/l4_passed/l4_total） | V0.4 | 当前硬编码 0/0 |
| 4 | `docs/重构笔记-使用开发规范重构项目指南.md` | 待用户决定 | V0 遗留 untracked |
| 5 | 模板版 `src/coding/ruff.toml` 同步 | V0.4 | V0.2 清理后未同步 |

## 五、统计数据

- **提交数**（V0.3 期间）：7 commits
  - `b4223ae` V3.1 markdownlint 启用
  - `61d3efa` V3.2 render.py 数据 bug 修复
  - `06dcb74` V3.3 gitleaks cache disable + V3.4 L1 markdownlint
  - `6318fd2` V3.4 markdownlint 跨平台（npx 替代 .cmd）
  - `6fdd788` V3.3 render.py ruff S603/S607 豁免 + l1_check 同步
  - `9e7070e` V3.2 build 阶段 Render 步先 pip install pyyaml
  - `8226c2d` V3.2 render_date 改 static latest
  - `8d89b65` V3.2 render.py 写文件强制 LF（CRLF→LF 跨平台）
- **CI 总耗时**：40s（V0.3 收尾 `27092082957`）
- **L4 套件**：53 passed in 2.78s
- **pre-commit 钩子**：10 钩子（trim trailing whitespace / end-of-file-fixer / check yaml / check json / check large files / ruff / ruff-format / gitleaks / commit-msg-worklog-ref / commitlint / **markdownlint**）

## 六、签核

- [x] AI 收束：本收束节点 v0.3 关闭
- [x] CI 验证：5 阶段全绿（`27092082957`）
- [ ] 人审计：待签核（xiangbianpangde）

---

**下个收束节点**：V0.4 预计完成其他 5 篇规范 l1_check 扩展 + dashboard 模板字段 + 模板版 ruff.toml 同步。
