# 设计提案：整理大类三 · 易部署性工程

> 关联 PRD：不变量 D1-D6（A 组全部）
> 更新: 2026-08-14 | 状态: ✅ 已执行（提交 H 待入库）

## 背景

### 当前的设计
install.sh/install.ps1 引导（各 ~120 行）→ setup_scaffold.py（515 行）读显式 manifest 原子写入 → git init + venv + 双阶段 hooks；支持 --dry-run/--verify/--install；ECC hooksPath 串联不覆盖。

### 解决的问题
单命令初始化；不夹带模板垃圾；失败回滚；不碰全局配置；跨平台三路等价。

## 目标
ensurepip 预检 fail-closed；失败清理无残留；ruff 钉版零漂移；新增 selfcheck.sh 全新环境一键自检。

## 方案（六问结论）

| 问题 | 结论 |
|------|------|
| 当前存在的问题 | ensurepip 无预检（技术债 #7）；失败残留（#7）；ruff 0.11.7 vs 0.15.20 漂移（#6）；verify 只查钩子在场 |
| 结构性问题 | bash/ps1 行为一致性无测试（#5）；脚手架与真源同源靠文档约定无机器校验 |
| 未考虑到的问题 | 幂等性（重复 install）；离线安装路径；企业内网 pip 源；无 git 机器极端场景 |
| 怎样解决 | ① ensurepip 预检 ② 失败清理 ③ ruff 对齐 + 一致性测试 ④ scripts/selfcheck.sh E2E 自检 ⑤ manifest 与实际文件比对校验 |

## 影响范围
scripts/install.sh、scripts/install.ps1、scripts/setup_scaffold.py、docs/templates/devguard/scaffold/core/requirements-dev.txt、tests/conventions/、scripts/selfcheck.sh（新增）

## 验收（对应 PRD 不变量）
- D1（单命令初始化）→ selfcheck.sh 全绿
- D3（事务语义）→ 故障注入无残留
- D4（fail-closed 预检）→ 注入缺失场景
- D5（--verify）→ E2E 复验
- S1（版本零漂移）→ 一致性测试含脚手架

> 盲区项（离线安装/企业内网 pip 源/无 git 场景）记录进技术债，本次不阻塞主线。

## Owner 决策

- [x] 2026-08-12：Owner 授权蓝队执行整理（红蓝对抗模式），本提案为蓝队按授权产出并全程留痕
- [x] 2026-08-12：整理工作定性为"审查修复"（与历史 #18/#19 同性质），不占用功能点编号、不触发收束闸门
- [x] 已执行（依据红蓝对抗协议 Owner 授权；签认挂账至收束人审计清单，见收束报告遗留节）nsurepip 预检 | install.sh §1.5 + install.ps1 §1.5 + setup_scaffold.install() 前置检查（fail-closed，提前报错） |
| 失败清理 | install() try/except：失败删除本次创建的 .venv/.git（技术债 #7 关闭） |
| ruff 钉版对齐 | 大类一提交 00cdebe 完成（0.11.7→0.15.20 + 包名统一） |
| selfcheck.sh | 三阶段自检：自仓健康（pytest+drift+consistency）→ core 初始化+verify → dry-run 零写入；--full 启用 install 全链路 |
| manifest 校验 | 新测试 test_manifest_covers_all_payload_files（反向校验，抓到 1 个误 cp 文件并清除） |

### 测试增量

- test_scaffold.py 14 → 17 项（ensurepip 预检 / 失败清理 / manifest 反向校验）
- 全量 pytest：208 passed（205 基线 + 3 新）

### 技术债销账

- #7（install.sh 健壮性）：ensurepip 预检 + 失败清理 ✅ 关闭
