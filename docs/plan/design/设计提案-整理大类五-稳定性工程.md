# 设计提案：整理大类五 · 稳定性工程

> 关联 PRD：不变量 S1-S7（C 组全部）
> 更新: 2026-08-14 | 状态: ✅ 已执行（提交 J 待入库）

## 背景

### 当前的设计
_meta.yaml 工具链真源 + check_consistency.py（矩阵 ≥80% 门槛）+ check_enforcement.py（故障注入 ≥10 项 / 拦截率 ≥90%）+ render_meta.py --check + 205 测试基线。

### 解决的问题
版本有单一真源；渲染产物禁手改；强制力量化；收束机器闸门。

## 目标
环境一键可复现（bootstrap.sh）；本仓 .venv 修复且 205 测试全绿；一致性门槛提升；CI 跨平台 job。

## 方案（六问结论）

| 问题 | 结论 |
|------|------|
| 当前存在的问题 | **本仓 .venv 无 pytest，测试不可运行**（环境不可复现活证据）；ruff 漂移（#6 见大类三）；无跨平台 CI（#5） |
| 结构性问题 | 可复现环境靠口头承诺无重建脚本；一致性门槛 80% 偏低（自查 100%） |
| 未考虑到的问题 | requirements 无 hash 锁定（供应链）；Python 版本只测单一版本；CI 第三方 action 未 pin |
| 怎样解决 | ① scripts/bootstrap.sh 重建环境 + 版本指纹 ② 立即修复本仓 venv ③ 门槛 80%→95%（先量现状）④ CI 跨平台 job ⑤ hash 锁定入技术债 |

## 影响范围
scripts/bootstrap.sh（新增）、.github/workflows/ci.yml、scripts/check_consistency.py、requirements-dev.txt、pyproject.toml

## 验收（对应 PRD 不变量）
- S1（版本零漂移）→ check_consistency 全绿
- S2（测试可复现）→ bootstrap.sh 全新重建后 205 全绿
- S3（故障注入 ≥90%）→ check_enforcement 保持
- S4（渲染产物禁手改）→ render_meta --check 保持

> 供应链 hash 锁定记录进技术债，本次不阻塞主线。

## Owner 决策

- [x] 2026-08-12：Owner 授权蓝队执行整理（红蓝对抗模式），本提案为蓝队按授权产出并全程留痕
- [x] 2026-08-12：整理工作定性为"审查修复"（与历史 #18/#19 同性质），不占用功能点编号、不触发收束闸门
- [x] 已执行（依据红蓝对抗协议 Owner 授权；签认挂账至收束人审计清单，见收束报告遗留节）nv + 固定依赖 + install-hooks + 测试全绿 + 版本指纹（importlib.metadata） | ✅ |
| 本仓 .venv 修复 | 大类一前置完成（pre_commit 4.6.0 / pytest 9.1.1 / ruff 0.15.20，指纹与 requirements 钉版一致） | ✅ |
| 一致性门槛 80→95 | check_consistency DEFAULT_THRESHOLD 95 + _meta.yaml l1_check 描述 + ci.yml --threshold 95（根/模板）+ 文档同步；现状 24/24=100% 达标 | ✅ |
| CI 跨平台 job | test-cross-platform（macOS+windows matrix，fail-fast:false，排除 test_installer/test_dashboard_entry 两个 shell 依赖测试）——技术债 #5 关闭 | ✅ |
| 供应链 hash 锁定 | **本次不做**：dev 依赖风险低，hash 锁定增加维护成本 | ⏸️ 技术债 #10 |

### 测试增量

- test_consistency.py 期望同步（score < 95）
- 全量 pytest：208 passed
