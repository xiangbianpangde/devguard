# V2.3 批次收口 — 全量回归 + 口径对齐 + dashboard 刷新

> 更新: 2026-06-11 | 范围: V2.2 #47 + V2.3 全批次（#48-#54 + 阶段C 补遗）| 状态: ✅ 已完成

## 做了什么

V2.3 最后一件（#49）交付后的批次收口：全量回归 + 跨文件口径终核 + dashboard 重渲染。

- [x] 全量回归绿：`pytest tests/conventions/` **131 passed**；L1 全套 OK（check_status / check_claude / check_plan 46↔46 / check_ai_workflow 9 篇 / check_html_artifact --all 29 受查 / check_decision_alignment --all 3 提案 0 待拍板）；`audit_file_placement` 375/375 合规；`render_meta --check` pre-commit-config 一致
- [x] 豁免审计：历史用过的 4 个标记全部已登记；V2.3 规划的 9 个标记目录齐（含 `[skip-html]`/`[skip-align]`/`[skip-docsync]`）
- [x] dashboard.html 按最新 STATUS + L4 131/131 重渲染（CI build 阶段有 drift 检查，须与渲染产物一致）
- [x] STATUS 累计数据刷新（19 钩子 / 131 L4 / V2.3 脚本 9 个 / commits 计数）
- [x] 确认收束闸门状态：`nodes=46,54 last_converged_fp=46`，已交付最大功能点 = #54 = 预设节点本身，**未越过**——批次内交付全部放行，#55+ 将被拦直至人收束

## 关键决策

- **dashboard「0% / 0/25」保留**：当前进度表行是版本批次行而非功能点行，render.py 按行算完成度得 0%——HEAD 上既有口径怪癖（之前显示 0/15），非本批引入；归入技术债不在收口私改渲染语义。
- **AI 不替人收束**：V2.3 批次全交付即停在收束节点 #54 前；四阶段（整理/测试/审计/验证 → ADR + 收束报告）等待人触发（08-汇报收束 §6.7）。

## 遇到的问题

- 本批三次提交被钩子打回的共性教训已沉淀：① pinned ruff-format(v0.6.0) 与本地 ruff 格式不一致——提交前用 `pre-commit run ruff-format --all-files` 对齐；② commitlint 把含 `#N` 的长 body 段判为 footer（footer 行宽 100 硬限）——body 拆短行；③ 中文主题若拉丁字母只有大写 V 会触发 subject-case 全大写误判——主题须含小写拉丁词。

## 验证结果

- 回归矩阵（本日终态）：131 L4 passed / 19 pre-commit 钩子（commit-msg 11）/ 1 PostToolUse / 1 liaison agent / CI 7 个 check step（compliance +2：html-artifact、decision-alignment）
- 全流程强制覆盖（ai-workflow → 工具拦截映射）：汇报不断档=#36-#38；文件放置=#45；豁免留痕=#51；更新时间=#53/#54；收束节点=#52 节点闸门；文档同步=阶段C doc-sync；决策对齐/不越界=#50；HTML 产出物=#48；可外溢=#49 脚手架
- 仓库干净：工作树 0 未跟踪/未暂存（提交前）

## 下一步

- [ ] **人触发收束节点 #54**（四阶段 → ADR + 收束报告 → 上调 `last_converged_fp=54` 释放闸门）——AI 已到闸门拦截位，不替人执行
- 人审计签核 v0.2-v2.1（14 节点）目标 2026-06-30 前补齐（人侧待办）
- 技术债不变：render_meta convention-grade 噪音（exit 0 不阻断）/ dashboard 完成度口径
