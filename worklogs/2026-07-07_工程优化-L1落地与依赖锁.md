# 2026-07-07 · 工程优化（P1：L1 落地 + 依赖锁 + dashboard 自动收集 + 钩子警告）

> 关联：设计提案-devguard一致性修复与工程优化.md（P1）
> Commit：待填入

## 完成了什么

- [x] P1-2：`requirements-dev.txt`（pip freeze 锁版本，避免本地 ruff 0.15 vs CI 0.6 漂移）
- [x] P1-3：`scripts/render_dashboard.sh` + `package.json` dashboard 改自动收集（`collect_l4_stats.py`，取代写死 53/53）
- [x] P1-4：`importlinter.ini` 修复（layers 完整模块路径 + include_external_packages + DDD 依赖倒置顺序），01-architecture L1 真正运行（2 kept）
- [x] P1-5：`src/api/openapi.yaml` 创建 + spectral lint 0 errors，04-api L1 真正运行
- [x] P1-6：`ci.yml` lint 阶段 + `_meta.yaml` ci 节接入 importlinter + spectral
- [x] P1-7：`_meta.yaml` commitlint 钩子去掉 `config` 键（消除 Unexpected key 警告），render 同步

## 验证

render --check ✅ / check_ai_workflow 9 篇 ✅ / compliance ✅ / importlinter 2 kept ✅ / pytest 69 ✅ / ruff ✅ / markdownlint ✅ / spectral 0 errors ✅

## 关键决策

- importlinter layers 改 DDD 顺序（presentation→application→infrastructure→domain）：infrastructure 实现 domain 接口（依赖倒置），domain 最下层
- openapi.yaml 全 inline（无 $ref/nullable）：规避 spectral-cli nimma 引擎遍历 $ref 的 bug
- commitlint config 键去掉：commitlint 自动读 commitlint.config.js，config 键反触发钩子版本警告

## 下一步

- [ ] pre-commit-hooks deprecated stage 名警告（不影响功能，待 autoupdate）
- [ ] spectral 5 个 warning（servers/contact/global-tags，建议性）
