# 工作日志：规范正文 v2.0 重构（从"纸上谈兵"到可落地）

> 日期: 2026-05-27
> 功能点: #34 规范正文 v2.0 重构
> 关联 BDD: docs/specs/01-08

## 做了什么

用户反馈现有规范"写得很差、纯纸上谈兵"，并指出方向：规范应是 `ai-workflow_AI协作开发流程/` 各步的**细化**，而非平行的独立文档。据此重写 7 份规范正文 + 同步治理文件。

### 1. 重写 7 份规范正文（01-06、08）
统一升级为「细化挂接 + 红线 + 落地配置 + 决策表 + 检查清单」骨架：
- **细化挂接**：每篇开头声明它细化 ai-workflow 哪一步（01→第一步设计；02→第二步§2.1实现/§2.3审查；03→§2.6提交；04→§2.1接口；05→§2.1 TDD/§2.2验证；06→§2.5/07-汇报；08→§2.9工具增强）。检查清单即对应环节审查清单的展开。
- **红线配自动检测**：每条红线标注怎么被工具拦（ruff 规则 ID / gitleaks / import-linter / commitlint / spectral）。
- **可复制落地配置**：pyproject.toml、.pre-commit-config.yaml、importlinter.ini、commitlint+.gitmessage+PR模板、统一响应拦截器+.spectral.yaml、覆盖率门禁。
- **可调阈值**：把"≤50行/≤7成员"等绝对值降为 linter 警告阈值。
- **档位裁剪**：轻量只守红线，标准/团队守全部。

### 2. 跨文件去重
- 密钥硬编码：权威归 02，01 改为引用。
- 调用图/CALL_GRAPH/@entrypoint/影响分析/代码地图/依赖黑洞：从 01/02/05/06 **全部移除**，收拢进 08 作为唯一权威。
- 图谱研究腔：08 删除全部"报告§x.x"、"VulGraph 91.2%/92%/89%/57%"等固化数字与 LLM Wiki，改为"见 docs/research/，不固化"；新增"仅中大型项目启用"门槛。

### 3. 治理同步
- 7 份 BDD 规格（docs/specs/01-08）：修正已迁移内容的断言、删除固化数字、新增"细化声明/红线检测/落地配置"场景。
- README-规范导航：索引加"细化的流程环节"列，红线速查去重（图谱红线并入架构）。
- STATUS.md / 开发清单.md：登记功能点 #34。
- CLAUDE.md：当前状态 33→34，加规范版本说明。
- FILE_GRAPH.md：注明本次仅改内容未动文件结构。

### 4. 跟进项（用户指示"推进"后追加）
- **ai-workflow/04 §2.9 对齐 08**：删 "Token 57%" 固化数字、移除 LLM Wiki，加"仅中大型项目引入图谱、详见 08"指引。
- **示例代码补齐落地配置**：让每个规范 §二「落地」的复制即用片段都有可运行的 src/ 对应物：
  - `src/coding/ruff.toml`（红线 1/4/5 检测）
  - `src/architecture/importlinter.ini`（分层契约）
  - `src/api/.spectral.yaml`（OpenAPI lint）
  - `src/testing/pytest.ini`（覆盖率门禁）
  - `src/git/.pre-commit-config.yaml` 由 black/flake8/isort **改为 ruff + ruff-format + gitleaks + commitlint**（与新 02/03 规范一致）
  - 同步更新 6 份 src README + 总览索引（新增"落地配置"列）
- **升级 `docs/templates/CLAUDE模板.md`**：原为薄骨架，重写为完整 AI 上下文模板——含跨维度红线摘要表（配自动检测）、工作流程、收束闸门、文件放置规则；新项目复制后其 AI 即知如何遵循 v2.0 规范。
- **刷新根 `CLAUDE.md`**：加入"规范=流程细化"设计、规范速查表、去重原则、src 落地配置引用、状态升 v2.0。
- **新建 `conventions/CLAUDE-规范导航.md`**：面向 AI 的导航（与人类版 README-规范导航 配对）——任务→规范定位、红线总表、单一权威/去重映射、改规范同步清单；已登记 FILE_GRAPH。

### 5. 汇报定义澄清（用户反馈后固化进规范）
用户明确：**功能点级汇报在对话里内联说即可，不单独建 `docs/reports/` 文档**；只有收束报告落盘。把这条写进所有相关文档，避免后续误会：
- 权威 `07-汇报.md`：§二/§三/§五 均标注功能点汇报"对话内联、默认不落盘"，收束报告"必落盘"。
- ai-workflow `04 §2.8`、`05`、`01`：同步措辞。
- `docs/templates/汇报模板.md` 档位一、`docs/reports/README-汇报说明.md`、`docs/specs/10`（含"四档"→"两档"遗留修正）。
- 根 `CLAUDE.md`（工作流程表 + 速查）、`docs/templates/CLAUDE模板.md`、`AI协作提示词模板.md`。
- 已存记忆 `feedback-reporting-inline`。

## 验证结果
- **规范正文**：7 份（01-06、08）全部重写。
- **BDD 规格**：7 份（specs/01-08）realign。
- **示例代码**：新增/对齐 5 份落地配置（ruff.toml / importlinter.ini / .spectral.yaml / pytest.ini / .pre-commit）+ 6 份 src README + 总览索引。
- **AI 上下文文件**：根 CLAUDE.md 刷新、CLAUDE模板.md 升级、新建 conventions/CLAUDE-规范导航.md。
- **治理**：README-规范导航、STATUS（34/34）、开发清单（#34）、FILE_GRAPH、本 worklog。
- 自检（grep）：调用图主题在 01/02/05/06 已无独立章节，均一句话指向 08；conventions 内已无"报告§""91.2%/VulGraph/LLM Wiki"等字样；src 内仅余"取代 flake8/black"的说明性注释。
- 篇幅显著下降（如 02 由 381 行降至 ~150 行）。
- 待用户人工通读确认风格与可操作性。

## 关键决策
| 决策 | 原因 | 影响 |
|------|------|------|
| 规范定位为 ai-workflow 的"细化"而非平行文档 | 用户明确纠正：是细化不是模仿 | 每篇挂接具体流程步骤，检查清单=审查清单展开 |
| 调用图内容全部收拢进 08 | 同一主题原散落 5 处，改一处要改五处 | 08 成唯一权威，其余仅引用 |
| 删除图谱固化数字、设启用门槛 | 数据来路不明像营销，且小项目不需要 | 08 降为务实建议，避免过度设计 |
| 登记为普通功能点 #34，不触发收束 | 用户明示"只是功能，远未到收束点" | v0.1 收束闸门不受影响 |

## 给下一位的交接
> - 样板是 02，其余 6 篇照同一骨架铺开——若再改风格，以 02 为基准同步全部。
> - 落地配置里的工具版本号（ruff/gitleaks/commitlint 的 rev）是示例值，真实项目落地时应锁定到当时最新稳定版。
> - 03-git BDD 新增的是"场景 0"（编号刻意为 0 以免与既有场景冲突），04 同。
> - 下一个收束节点触发时，本次重构的关键决策应提升为 ADR。
