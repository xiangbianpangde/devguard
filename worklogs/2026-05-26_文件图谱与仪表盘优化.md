# 工作日志：文件图谱 + 仪表盘优化 + 收尾修复

> 日期: 2026-05-26
> 功能点: #20 文件图谱与仪表盘优化

## 做了什么

### 1. 修复二轮审查遗留的 3 个小问题

1. **spec 编号跳号 (08 缺失)**：`git`/`mv` 重排 `09-examples → 08-examples`、`10-dashboard-gen → 09-dashboard-gen`，specs 现连续为 00-09；同步更新 `STATUS.md`、`plan/开发清单.md`、`CLAUDE.md`、相关 worklog 的引用。
2. **示例库组织方式易混淆**：新增 `src/README.md`，明确 `src/` 按规范维度分目录（便于对照），**不是**推荐的产品结构；产品结构见 01-架构规范。
3. **`docs/design/` 被规范建议但不存在**：创建 `docs/design/README.md`（用途 + 五段式设计文档模板）。

### 2. 文件架构图谱（新）

新增 `meta/FILE_GRAPH.md` —— 文件归类权威，含三部分：
- 带职责标注的目录树
- 引用关系图谱（谁依赖谁 + 关键同步约束）
- "新增文件放哪"决策树（树状推理）

`CLAUDE.md` 增加指引："新增文件前先查 `meta/FILE_GRAPH.md`，禁止根目录堆散文件"；目录树同步补 `meta/`、`docs/design/`、`start_server.py`、`打开仪表盘.bat`。

### 3. 仪表盘优化 (`dashboard.html`)

在不破坏 BDD 约束（纯静态 / 单次 `fetch('STATUS.md')` / 自动刷新）的前提下重写：
- **原地刷新**取代整页 `<meta refresh>`，无闪烁、保留滚动与筛选
- 新增**状态分类统计 chips**（已完成/进行中/待开始/阻塞）+ 可点击**筛选**
- BDD 列变为**指向 `docs/specs/` 的可点击链接**
- 顶部显示从 STATUS.md 解析的**真实更新日期** + **下次刷新倒计时** + **手动刷新按钮**
- 解析器更健壮（按 `##` 分节，图例表不再误入功能点）

### 4. 顺手清理

- `.gitignore` 补 `.benchmarks/`（pytest-benchmark 产物）
- `start_server.py` 启动后自动打开浏览器；新增 `打开仪表盘.bat` 双击启动器
- 修正 README 中关于刷新机制的过时说明

## 验证结果

- 全仓库 Markdown 链接检查：无死链
- 复刻新解析器跑 STATUS.md：19→20 功能点全部正确解析，图例行未泄漏，技术债/更新日期提取正确
- HTTP 服务抓取 dashboard / STATUS / FILE_GRAPH / 重命名后的 specs / 新 README：全部 200
- 示例代码回归：`pytest` 13/13、架构 demo `final_total` 正确

## 给下一位的交接

- 20 个功能点全部完成。
- 新增文件务必先查 `meta/FILE_GRAPH.md` 决策树归位。
- 技术债仍只一项：项目自身无 CI/CD pipeline。
