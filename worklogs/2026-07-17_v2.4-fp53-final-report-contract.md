# 工作日志：#53 报告格式入族（final-report 契约 + 规范路由澄清）

> 日期: 2026-07-17
> 功能点: #53 报告格式入族（final-report 契约 + 规范路由澄清）
> 关联 BDD: docs/specs/10-templates-reporting.md

## 做了什么

依据 Owner 已拍板的设计提案 `docs/plan/design/设计提案-报告格式入族与一键部署性提升.md`，把 final-report 报告格式从"约定俗成"升级为模板族硬契约，并澄清规范文档中"内容要素 vs 版式标准"的路由。

契约锚点（五件套）：`class="hero"` / `class="kpi-row"` / `class="toc"` / `class="verdict"` + ≥2 个 `class="mermaid"` 块。

- `scripts/check_html_artifact.py`：新增 `MERMAID_BLOCK`、`FINAL_REPORT_MIN_MERMAID=2`、`FINAL_REPORT_CLASS_ANCHORS`、`CLASS_CONTRACTS={"final-report": ...}` 与公开函数 `final_report_anchor_errors(content)`；`check_content` 改为 TYPE_CONTRACTS 与 CLASS_CONTRACTS 双查——带 `doc-template="final-report"` 的文件在通用 DOCTYPE/title/`<nav` 校验后走类锚点分支，缺任一锚点即 FAIL；未打标文件维持 WARN-only 不变。
- 打标 3 份 canonical HTML（viewport meta 后插入 `<meta name="doc-template" content="final-report">`）：`docs/templates/devguard/final-report-template/template.html`、`demo.html`、`docs/reports/2026-07-17_devguard_审查修复_验收报告.html`。`merged_report.html` 刻意不打标（存量迁移留 #54）。
- `tests/conventions/test_html_artifact.py`：新增 `TestFinalReportContract` 7 个测试（契约注册、4 份 canonical 参数化实测、打标文件通过、缺锚点×4、1 个 mermaid FAIL/2 个通过、未打标仍 WARN-only），先红 7 failed 后绿。
- 规范路由澄清 6 份：`04-长程开发.md` §5.2 改为「内容要素 vs 版式标准」三分法（8 要素=内容、final-report=默认版式、汇报模板.html=轻量）；`08-汇报收束.md` §1.3 加版式路由段；`final-report-template/README.md` 加「类型契约（V2.4 #53）」节；`docs/templates/汇报模板.html` 加轻量定位注释；`README-模板索引.md` 加「HTML 模板族类型契约」表；`docs/specs/10-templates-reporting.md` 新增「场景 5：final-report 纳入模板族硬契约（V2.4 #53）」（原场景 5 顺延为场景 6）。

完成项：

- [x] check_html_artifact.py 支持 final-report 类锚点契约（5 锚点硬校验）
- [x] 3 份 canonical HTML 打标 `doc-template="final-report"` 并实测通过
- [x] TestFinalReportContract 7 测试先红后绿
- [x] 6 份规范/模板文档路由澄清
- [x] STATUS / 开发清单 / CLAUDE 三处同步 53/53

## 验证结果

- `pytest tests/conventions -q`：**188 passed**（新增 7 个，全量无回归）
- 4 份 canonical 实测锚点全满足：template/demo/merged_report mermaid 各 11/11/18 个，2026-07-17 报告 4 个；verdict 用 `class="verdict"`（非 id，template/demo/merged 无 `id="verdict"`）
- `check_html_artifact.py --all`：WARN 24 → **23**（2026-07-17 验收报告打标后退出 WARN 名单；merged_report 仍 WARN-only）
- convergence-gate 未触发：节点表 `31,35,46,49,52`，52 之后无 pending 节点，#53 非收束功能点、未声明新节点

## 关键决策

| 决策 | 原因 | 影响 |
|------|------|------|
| 锚点用字面量子串匹配（`class="hero"` 等） | 与现有 `check_content` 风格一致，无 HTML 解析依赖；4 份 canonical 已实测全部满足 | 模板作者需保持标准 class 写法，README 已写明 |
| verdict 用 class 不用 id | template/demo/merged 三份 canonical 均无 `id="verdict"`，class 是唯一共同锚点 | 契约锚点定为 `class="verdict"` |
| merged_report.html 不打标 | 存量迁移属 #54 范围，本功能点不夹带 | 该文件继续 WARN-only，#54 处理 |
| 不声明新收束节点 | #53 非收束功能点；gate 规则为「交付数越过 pending 节点」才拦截，52 后无 pending 节点 | 是否增设节点（如 57）留待下次收束或 Owner 拍板 |

## 给下一位的交接

> #54 存量迁移：`docs/reports/merged_report.html` 打标前需先补齐 5 锚点（目前缺 hero/kpi-row/toc/verdict 中的一部分，mermaid 18 个已超下限）。check_html_artifact.py 的 FAIL 指引文案已含 final-report 说明，新模板作者照 `final-report-template/README.md` 的「类型契约（V2.4 #53）」节即可。
