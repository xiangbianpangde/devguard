# 2026-07-11 · P0 可测一致性与强制闸门

> 关联：功能点 #48
> Commit: 当前功能分支提交（以 `git log` 为准）

## 完成了什么

- [x] 建立 24 项一致性事实矩阵：进度真源、规范↔BDD、hook 绑定、CI↔模板投影、格式器版本一致、可复现渲染和真实故障注入。
- [x] 建立 10 项隔离 Git 故障注入：worklog、STATUS、结构、放置、豁免、日期、同步和收束越界。
- [x] 把 8 个治理 gate 接入 commit-msg，并用 `meta/豁免清单.md` 管理全部 `[skip-*]`。
- [x] 修正 STATUS/开发清单/CLAUDE 的 49/46/35 漂移，编号与机器标记同源。

## 验证结果

- `check_consistency.py --threshold 80`：24/24，100.0%。
- `check_enforcement.py --threshold 90`：10/10，100.0%。
- 59 项聚焦 gate/渲染/脚手架测试通过；最终全量套件 128/128 通过。
- 收束后手工高风险 diff 审核修正 1 个 CRLF 文件，并把 CI `ruff` 锁定为与 pre-commit 相同的 0.6.0，启用 `ruff format --check`。

## 关键决策

- 分数只能由可复跑事实的 passed/total 计算；文件存在和主观估计不计分。
- 拦截必须命中预期 gate 和预期诊断；“因别的错误退出 1”不算正确拦截。

## 遇到的问题

- 首轮结构同步注入命中了 CLAUDE 缺失而非 STATUS↔计划漂移；修正 seed 后达到 10/10。
- 最终 `git diff --check` 发现单个测试文件混入 CRLF；已规范为 LF，并把 CI/模板漂移纳入评分，防止复发。
- 无 UTF-8 环境变量时，Windows 子进程输出会被按 UTF-8 误解码并在 GBK 控制台触发 `UnicodeEncodeError`；已固定子进程 UTF-8 契约并加回归测试。

## 下一步

- [x] 把门槛接入 CI 与收束流程。
