# 工作日志：二轮全量审查修复

> 日期: 2026-05-26
> 功能点: #19 二轮审查修复（全量文件复查）

## 做了什么

对全部文件做了二轮复查，发现并修复 8 个问题（1 个功能性 bug + 3 个文档失实 + 4 个整洁/可移植项）。

### 🔴 功能性 bug

1. **架构示例丢弃折扣**：`src/architecture/application/order_service.py` 调用 `DiscountCalculator` 算出折扣后，创建 `Order` 时未传入，`Order.total` 永远是原价。demo 自相矛盾（打印"最终价 480"却返回 `total: 600`）。
   - 修复：`Order` 增加 `discount` 字段 + `final_total` 属性；`OrderService` 写入折扣；控制器返回 `total`(原价) 与 `final_total`(折后)。

### 🟠 文档与现实不一致

2. **目录重命名从未落地**：`#18 基础设施修正` 声称已把 `ai-workflow_AI协作开发流程/` 改为 `07-` 前缀，但磁盘上一直是无前缀版本，导致 `README.md` 两处引用为死链。
   - 修复：`git mv` 真正执行重命名（对齐 spec 文件名 `07-ai-workflow.md` 与编号体系），同步修正 `CLAUDE.md`、`conventions/README.md` 的引用。
3. **CLAUDE.md 状态严重过期**：写"已完成 10 个，剩余 7 个"，实际 18 个全部完成。一并修正 specs 范围(00-07,09-10)、src 描述。
4. **示例库 BDD 场景3 验收点未满足**（spec 文件本轮已由 09 重排为 08）：要求 Python+TypeScript 双实现，实际为 Python-only。
   - 修复：改写为"Python 单一参考实现 + 规范正文覆盖语言差异"，与实际交付一致（决策见下表）。

### 🟡 整洁 / 可移植 / 一致性

5. **重复且有坑的模板**：`docs/templates/STATUS.md` 与 `STATUS模板.md` 并存，前者表头与 `dashboard.html` 解析器不兼容 → 删除前者。
6. **.gitignore 违反自身规范**：缺 `__pycache__/`、`*.py[cod]`、`.env*` → 补齐（对齐 02-coding / 03-git 规范）。
7. **start_server.py 不可移植**：硬编码绝对路径 + 绑 `0.0.0.0` → 改用脚本自身目录 + 绑 `127.0.0.1`。
8. **零碎过期**：`STATUS.md` 头部"功能点 17 完成" → 18；架构 demo 在 GBK 控制台中文乱码 → 入口 `sys.stdout.reconfigure("utf-8")`。

## 关键决策

| 决策 | 原因 | 影响 |
|------|------|------|
| spec 09 改为 Python 单一参考语言 | 文档模板项目维护 6 套 TS 镜像得不偿失，规范正文本就语言无关并已点明命名差异 | 验收标准与实现一致，不再误判为"未完成" |
| 实际执行 07- 目录重命名（而非回退 README） | spec 文件名、README、worklog 意图均指向 07- 编号，重命名最一致 | 编号体系 01-07 连续，死链消除 |
| 折扣下沉为 `Order.discount` 字段 | 折扣属订单状态的一部分，由应用层写入、领域层暴露 `final_total` | 既修 bug 又示范了"领域对象承载业务状态" |

## 遇到的问题

- #16 工作日志曾记"架构示例运行通过，分层调用流正确"——但折扣 bug 当时已存在，说明上一轮验证只看了是否报错、未核对业务结果。本轮通过比对 demo 输出数值发现。

## 验证结果

- `pytest src/testing/` — 13/13 通过
- `python -m src.architecture.main` — VIP 单现返回 `final_total: 480.0`（原误为 600），中文输出不再乱码
- `grep conventions/ai-workflow` — 仅剩 worklog 中描述"旧→新"的合法引用，无死链
- `start_server.py` — 语法校验通过

## 给下一位的交接

- 19 个功能点全部完成。
- 技术债仍只一项：项目自身无 CI/CD pipeline——若补上，可在 CI 中跑 `pytest` + 链接检查，能自动拦住本轮这类"文档声称的操作未落地""demo 数值错误"问题。
- 验证功能点时不要只看"是否报错"，要核对业务结果数值（本轮 bug 即栽在这里）。
