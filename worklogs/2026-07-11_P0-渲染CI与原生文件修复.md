# 2026-07-11 · P0 渲染、CI 与原生文件修复

> 关联：功能点 #47
> Commit: 待提交

## 完成了什么

- [x] `render_meta.py` 统一 LF + 单一 EOF 换行，只向 Markdown 注入分级标签。
- [x] 恢复 `.github/CODEOWNERS` 和 MIT `LICENSE` 的原生语义。
- [x] CI 改为检查已提交渲染产物，pytest/L4 收集失败闭合，不再伪造 `0/0`。
- [x] 合规扫描从文件存在性升级为非空、配置解析、Python 编译和真实 test 定义。

## 验证结果

- 聚焦红测：7 failed / 1 passed；修复后 9 passed。
- `render_meta.py --check`、CI YAML 解析、LF/EOF 字节检查和 `git diff --check` 通过。
- `check_compliance.py`：17 篇规范、66 个语义证据通过、0 失败。
- 最终全量回归：125 tests passed。

## 关键决策

- LICENSE/CODEOWNERS 是原生格式，不允许 Markdown 渲染器修改。
- 文档/契约仓不伪造业务代码覆盖率；以完整契约测试、事实矩阵和故障注入作为证据。

## 遇到的问题

- CI 原先在检查前先渲染，掩盖提交漂移；调整为只读检查。
- L4 解析失败原先回退 `0/0`；改为非零退出并保留旧 dashboard。

## 下一步

- [x] 接入一致性与强制性硬门槛。
