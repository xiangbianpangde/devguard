# 2026-07-11 · P1 一键初始化与跨平台仪表盘

> 关联：功能点 #49
> Commit: 待提交

## 完成了什么

- [x] 新增 core/optional 显式 manifest、自包含模板载荷和结构化初始化回执。
- [x] `setup_scaffold.py --install` 一条命令完成实例化、Git、`.venv`、固定依赖和双阶段 hooks。
- [x] 默认拒绝非空目标；manifest 源缺失、未解析变量、坏回执和临时文件均失败闭合。
- [x] 用 Python dashboard 入口替代 Bash，改为读取 49 条详细功能点和真实 pytest 数字。

## 验证结果

- 仓外 optional E2E：20 个 manifest 文件、联网安装、Git 初始化、双 hook 安装全部成功。
- 目标自身：`DEVGUARD OK`、2 tests passed、全部 pre-commit 通过、commit-msg 通过。
- 源侧 `--verify --require-hooks` 通过；临时目录经绝对路径边界校验后清理。

## 关键决策

- 新项目只走显式 manifest，不再递归复制包含历史素材的 `docs/templates/devguard/`。
- receipt 的 manifest/摘要格式永久校验；生成瞬间校验内容 hash，后续允许 Owner 正常修改实例文件。

## 遇到的问题

- 首次人工 E2E 在错误 cwd 运行 pre-commit，暴露了验证命令自身的问题；修正为目标目录后完整通过。
- dashboard 旧解析器显示 0/11；改为从编号详细表计算，与进度机器标记互检。

## 下一步

- [ ] GitHub `master` 分支保护待浏览器完成认证后配置并回读。
