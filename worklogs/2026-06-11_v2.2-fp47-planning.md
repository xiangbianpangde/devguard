# 2026-06-11 · V2.2 #47 立项 — ai-workflow 旧引用语义重映射 + 模板同步

## 完成了什么

- [x] 立项 V2.2 #47，登记到 STATUS.md（当前进度表 + 详细功能点列表，#47 待开始不打 fp 标签）
- [x] 立项 #47 详情写入 docs/plan/开发清单.md「V2.2 启动 — 漂移清理（#47）」段
- [x] V2.1 补漏（4 问题修复 + 漂移清理）已 commit：253a46c（fix ai-workflow）+ b4711dd（设计文档 + 2 份环境 worklog）

## 关键决策

- **#47 范围限定**：只处理两件 —— (1) conventions/01-08 + CLAUDE-规范导航 + docs/templates/ 内 12+ 处 V1.0 旧文件名死链 → 语义级重映射到 V2.0 新 9 篇；(2) 模板 `docs/templates/devguard/scripts/check_ai_workflow.py` V1.0 旧 7 篇硬编码 → 新 9 篇。用户决策。
- **#47 不处理项**：V2.1 #38 钩子对 `- [x]` 强制可能误伤 V1.x 风格 worklog（用户裁定"不行"，保持硬拦）。
- **为什么需要单独 #47 而非并入本批补漏**：本批 4 问题修复偏机械修（链接替换 / 标签打标 / 模型重设），#47 是**语义级重映射**——V1.0「第二步 §2.6 Git 提交」在 V2.0 结构里跨了「04-长程开发」（TDD 实现循环）+「03-设计规范」（设计文件夹 §2.5 更新记录）多篇，每个死链要逐条判断映射到哪一篇或哪几篇，不能机械 sed。这种语义判断值得单独立功能点、配 BDD、配 PR review，不应混入本批漂移修复。

## 遇到的问题

- 立项时要把 #47 明确**不放进当前 fp 标签计数**（待开始状态打 fp 标签会破坏 STATUS(46) ↔ 开发清单(46) 口径），STATUS 详细列表 #47 行保留无标签，开发清单 #47 行无标签。

## 下一步

- 新会话启动后，按 #47 的 BDD 起草流程：
  1. 全面扫描死链（12+ 处），逐条建「旧路径 → 新路径 + 语义说明」映射表
  2. 设计映射表归入 docs/plan/design/设计提案-旧引用重映射-v1.md（或挂在 M3-src 或 M1-conventions 下）
  3. 逐文件执行 Markdown 链接替换 + commit
  4. 同步模板 scripts/check_ai_workflow.py 旧 7 篇 → 新 9 篇
  5. 跑完整回归（86 L4 测试 + 3 L1 钩子 + 全树审计 340 文件）
  6. PR 评审 → 合入 main

## 验证结果

- check_plan.py：OK 开发清单 47 个功能点行，46 个 fp 标签 ↔ STATUS 46（口径一致；#47 待开始未打标）
- check_status.py：OK
- check_ai_workflow.py：OK
- audit_file_placement.py：OK 340/340 合规
- L4 测试：86/86 passed
- 最近 3 个 commit：b4711dd docs → 253a46c fix → bb6eaea V2.1
