# 工作日志：#55 单命令跨平台安装器（install.sh / install.ps1）— 补登

> 日期: 2026-07-22（补登；功能点交付日 2026-07-17）
> 功能点: #55 单命令跨平台安装器（install.sh / install.ps1 引导脚本）
> 关联 BDD: docs/specs/00-infrastructure-template.md
> 补登原因: 2026-07-22 三维审查发现 #55 声称已交付但产物从未入库（F01：install.sh / install.ps1 / test_installer.py 全 untracked、无 worklog），按 D1 决策补登本日志并完成提交闭环；断档事实不掩盖，补登日期如实标注

## 完成内容（做了什么）

- [x] `scripts/install.sh`（POSIX）/ `scripts/install.ps1`（Windows）引导脚本：探测 Python ≥3.10（缺失则分平台指引并退出，不自动安装、不静默降级）→ 定位本仓（当前目录 / `--repo` / `DEVGUARD_REPO`，定位失败 fail-closed 提示先 `git clone`）→ 参数原样透传 `scripts/setup_scaffold.py`
- [x] 支持 `curl -fsSL .../install.sh | bash -s -- <target> --install` 与 `irm .../install.ps1 | iex` 远程引导形态
- [x] `tests/conventions/test_installer.py` 16 项 L4 测试（探测/定位/透传/dry-run/fail-closed 路径）
- [x] README 三平台使用示例（bash / PowerShell / 直调 Python）与 `--dry-run`、`--verify --require-hooks` 说明
- [x] 2026-07-22 仓外真机复验（见下）

## 验证结果

- [x] `bash scripts/install.sh /tmp/devguard-e2e-fp55c --profile core --project-name 'E2E Probe' --install` → `HOOK INSTALL OK` + `INIT OK (core, 22 files)`（2026-07-22，anaconda python 3.12 + 清华镜像）
- [x] `python3 scripts/setup_scaffold.py /tmp/devguard-e2e-fp55c --verify --require-hooks` → `VERIFY OK (core)`（pre-commit 与 commit-msg 双钩已装、hooksPath 正确）
- [x] `--dry-run` 干跑：零写入列出完整 core 载荷清单，通过
- [x] pytest 全量 205 passed（含本功能点 16 项 installer 测试）
- [x] 环境负路径如实记录：① WorkBuddy managed python 3.13.12 的 `ensurepip` SIGABRT → 安装器 fail-closed 退出（不静默降级 ✓），但目标目录有已写入载荷残留（回滚未清目录，观察项）；② 默认 PyPI 直连 ReadTimeout → 换清华镜像后通过（网络环境非产物缺陷）

## 关键决策

- D1（2026-07-22 决策登记）：补登入库而非回退——产物真实、测试全绿、真机可复验；欠的是入库与汇报闭环

## 遇到的问题

- 交付日（07-17）完成后未提交：产物与四件套修改滞留工作树 5 天，直至 07-22 审查发现；根因是缺 worklog 导致按本仓闸门无法提交，形成死锁——已在审查报告 F01 立项，本轮解开

## 交接 / 下一步

- 本批产物随 C1 提交（feat(installer)），不 push，push 由 Owner 决定
- 观察项（已登记 STATUS 技术债 #6/#7）：脚手架载荷 ruff 钉版 0.11.7 与真源 0.15.20 漂移；install.sh 缺 ensurepip 预检且失败残留目录
