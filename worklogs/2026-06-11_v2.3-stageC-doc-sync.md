# V2.3 阶段C 补遗 check_doc_sync 四件套硬同步（#52 拆出项回收）

> 更新: 2026-06-11 | 功能点 #52 补遗（V2.3 阶段C）| 状态: ✅ 已完成

## 做了什么

把 #52 交付时拆出的 `check_doc_sync.py`（设计 §3.3 阶段C 第二件）补齐——四件套同步从 1 条边（worklog→STATUS，#37）扩到结构性 3 边（STATUS↔开发清单↔CLAUDE）。

- [x] 🆕 `scripts/check_doc_sync.py` commit-msg 钩子：编号功能点行（`| #N` / `| N |`）的**增/删**须 STATUS↔开发清单 两侧同步 + CLAUDE.md 同提交（硬拦）；**值级改动**（状态/日期翻转，行集不变）仅 WARN——严格执行设计 §1.4 分粒度，防"过紧耦合逼出豁免滥用"
- [x] **增量比对**（staged vs HEAD）：只拦本次提交引入的失同步，不追溯存量口径差（STATUS 1-35 历史行无编号属 fp 标签口径，不参与）——仓库不被历史差异冻结
- [x] `tests/conventions/test_doc_sync.py` 8 L4（行提取×2 + 同步规则×5 + skip）+ test_05 覆盖清单 +1
- [x] `_meta.yaml` 登记（渲染 19 钩子）+ 06-documentation l1_check_path；豁免清单 §二 登记 `[skip-docsync]`（V2.3 规划豁免标记至此全部登记完毕）
- [x] **STATUS 残留 #52 ⏳ 行解决**：旧行（"长程任务流程强制规范操作…cron self 监控/断档防护"）与 #52 ✅ 行（收束硬闸门）重复且过期——改写为本补遗的交付行，与开发清单单行 #52 口径对齐

## 关键决策

- **原 #52 宽描述的三件事收口**：① 收束闸门 → #52 已交付（节点驱动）；② check_doc_sync → 本补遗交付；③ cron-self 监控属 mavis-team 运行时操作（CLAUDE AI 协作原则 4 的 L3 规则，无仓内工具拦截点）、断档防护已由 #36-#38（worklog 引用/同步/结构三钩子）焊死——不再另立功能点。
- **键 = 编号功能点行**：#36 起的编号行是 STATUS/清单共同的机器可读键；值级语义（状态翻转）不进硬拦，避免设计 §1.4 警告的噪音。

## 遇到的问题

- STATUS 详细列表与开发清单的历史口径差（1-35 无编号 + 衍展规范行不计数）决定了"全量一致性"不可检——采用与 #45/#52 相同的"只查增量"原则后问题消解。

## 验证结果

- `pytest tests/conventions/` → **126 passed**（+8）；sync_errors 五分支（增缺/双增过/删残留/缺 CLAUDE/值级零误报）全部单测覆盖
- 钩子对当前真实暂存区跑 → exit 0（无结构性增删时零摩擦）；`[skip-docsync]` 子进程放行
- `render_meta.py --check` → pre-commit-config 一致（19 钩子）；`ruff check` / `ruff format` 通过

## 下一步

- [ ] #49 基准约束脚手架（V2.3 最后一件，打包含本钩子的全部约束基线）
- V2.3 全交付后到达预设收束节点 #54 → 提醒人触发收束
